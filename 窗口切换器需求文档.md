# zeroflow — 内置 ⌘⇥ 窗口缩略图切换器需求文档

> 版本：v1.0
> 状态：需求定稿，待实现（由实现方按本文档落地）
> 关联文档：`需求文档.md`（主需求）、`AGENTS.md`（构建与架构速查）
> 参考实现：AltTab — https://github.com/lwouis/alt-tab-macos （下文所有「AltTab:」路径均指其仓库内文件）

---

## 1. 功能概述

### 1.1 产品定位

zeroflow 增加一个「窗口切换器」模块：接管系统 **⌘⇥（Cmd+Tab）** 应用切换快捷键，改为显示 **窗口缩略图面板** 供用户选择切换，行为与 AltTab 一致。系统 Cmd+Tab 只能切 app、看不到具体窗口，本功能把它替换成「每个窗口一张缩略图」的切换器，并支持切换到系统 Cmd+Tab 切不到的窗口。

### 1.2 能切到哪些系统 Cmd+Tab 切不到的窗口

| 场景 | 系统 Cmd+Tab | 本功能 |
|---|---|---|
| 同 app 有多个窗口（如 3 个 Finder 窗口） | 只能切 app，无法选具体窗口 | 每个窗口一张卡，可精确选 |
| 已最小化到 Dock 的窗口 | 切 app 后窗口还在 Dock | 列表给出缩略图卡，选中后自动还原 |
| 隐藏（⌘H）app 的窗口 | 不显示 | 正常列出 |
| 其他 Space 上打开的窗口 | 切不到 | 列出；选中后自动切到该 Space |
| 无窗口的 app | 不显示 | 默认不列（与 AltTab `showWindowlessApps = .hide` 一致，见下方判据说明） |

> **判据与 AltTab 一致**：以上覆盖范围与 AltTab 默认行为（AltTab: `src/switcher/state/WindowFilterResolver.swift` 的 `shouldShow`）**完全对齐，不额外扩大覆盖**——AltTab 默认不列出的窗口，本功能同样不列、也不尝试切：
> - 无窗口且非活动的后台 app（AltTab 默认 `showWindowlessApps = .hide`）→ 不列；
> - 被系统判定为 phantom 的窗口 → 不列；
> - 被用户设置忽略名单排除的 app → 不列（第一版可不做忽略名单，见 FR-23 预留）。

### 1.3 目标交互（手感与系统 ⌘⇥ 一致）

> 触发快捷键**可在设置中修改，默认 ⌘⇥**（见 FR-15/FR-21）。下文以默认 ⌘⇥ 描述交互；改为其他组合键（如 ⌥⇥）时逻辑不变，只是「按住 ⌘」换成「按住配置的修饰键」。

```
按住 ⌘ → 按 ⇥ → 弹出缩略图面板（系统切换器被完全吞掉，不出现）
     → 继续按 ⇥  前进一格 / ⇧⇥ 后退一格 / Esc 取消
     → 松开 ⌘ → 激活选中窗口，面板收起
```

- 按住 ⌘ 后**每按一次 ⇥** 前进一格（含按住自动连发）。
- **⇧⌘⇥** 后退一格。
- 松开 ⌘ 立即切换（不再需要再按一下）。
- **Esc** 取消切换：只关闭面板，不改变前台 app。
- 面板打开期间鼠标可悬停/点击某个窗口卡直接切换（点击即切换）。

### 1.4 术语

| 术语 | 含义 |
|---|---|
| 切换会话 | 从 ⌘⇥ 触发面板弹出，到松 ⌘/Esc 面板销毁的一段生命周期 |
| 窗口卡（Tile） | 面板中每个窗口的展示单元：缩略图/图标 + 标题 + app 名 |
| 最小化窗口 | 已 miniaturize 到 Dock 的窗口（`kAXMinimized=true`） |
| 系统切换器 | macOS 自带的 app 图标切换器（Dock 负责处理 ⌘⇥） |

### 1.5 性能与质量目标

- 热键到面板可见 ≤ 200ms（参照主需求「快捷键到选区 ≤ 300ms」）。
- 缩略图抓取不卡界面：全部在后台，面板先显示占位图标再逐张刷新。
- 同一会话内相同窗口不重复抓图；再次召唤时优先复用缓存（TTL 见 3.3）。
- 驻留内存增幅受控（缩略图按窗口卡分辨率、有上限并回收），不破坏「驻留 ≤ 80MB」目标。
- 模块关闭时**完全**恢复系统默认 ⌘⇥ 行为，零残留。

---

## 2. 整体工作流程

```
菜单栏「设置 → 切换」标签页启用开关
     │  （cmdTabSwitcherEnabled = true）
     ├─ 启动「切换」模块：安装 ⌘⇥ 事件 tap（需辅助功能权限）
     │
     ├─ 用户按 ⌘⇥
     │     ↓ 事件 tap 吞掉 Tab/Cmd keyDown/up（系统切换器不出现）
     │     ↓ 枚举窗口列表（CGWindowList 公开 API）→ 按 app 分组
     │     ↓ 弹出缩略图面板（NSPanel .screenSaver 非激活层）
     │     ↓ 后台批量抓取各窗口缩略图（SkyLight 私有 API，最小化窗口也能抓）
     │     ↓ ⇥/⇧⇥/Esc/鼠标悬停/点击 操作选择
     │     ↓ 松开 ⌘ → AX 还原+前置+激活选中窗口 → 面板收起
     │
     └─ 设置页「切换」标签页：开关 + 辅助功能权限引导(绿=已授权/橙=引导)
```

---

## 3. 功能需求（FR）

> 编号延续主需求文档（主需求已到 FR-14）。

### FR-15 功能开关与入口 【新增】

- **FR-15.1** 「窗口切换」（模块名）总开关，key：`cmdTabSwitcherEnabled`，默认**关**。
- **FR-15.2** 打开开关即安装 ⌘⇥ 事件 tap，立即生效；关闭即移除 tap，系统 ⌘⇥ 完全恢复，无残留。
- **FR-15.3** 总开关关闭状态下：不安装 tap、不弹面板、不抓窗口、无任何行为。
- **FR-15.4** 开关与截图功能无关，可独立开启/关闭；与 Dock 单击最小化模块互不干扰（两者各自独立 CGEventTap，可共存）。
- **FR-15.5** 打开开关时若未授权辅助功能权限，自动弹出系统授权（`AXIsProcessTrustedWithOptions`，与 Dock 模块一致）；授权返回后模块自动生效，无需重启。
- **FR-15.6** 触发快捷键**固定两选一**（无自由录制），默认 `⌘⇥`（Command + Tab）。key 见 §4：`cmdTabShortcut.keyCode` + `cmdTabShortcut.modifiers`；两个固定搭配：
  - `⌘⇥`：keyCode 48（kVK_Tab）+ `.command`（默认）；
  - `` ⌥` ``：keyCode 50（kVK_ANSI_Grave，Tab 上方 / 数字 1 左侧那枚 ` 键）+ `.option`。
  - 加载时若持久化的是「两选一」之外的旧值（此前自由录制遗留），一律钳回默认 `⌘⇥`。修改后立即生效（事件 tap 重新读取配置）。
- **FR-15.7** 不提供自由录制快捷键功能：切换页用固定 `Picker(.radioGroup)` 二选一，因而**不再需要**录制期间挂起/恢复本模块 tap（`suspend()`/`resume()` 已删除，与截图页 `GlobalHotkeyManager.suspend/resume` 不同，后者仍保留供截图快捷键录制使用）。

### FR-16 ⌘⇥ 拦截 【新增】

- **FR-16.1** 用 `CGEvent.tapCreate`，参数固定为：
  - `tap: .cghidEventTap`（HID 级最早介入点，能赶在系统/Dock 之前处理 ⌘⇥）
  - `place: .headInsertEventTap`
  - `options: .defaultTap`（**必须** defaultTap 才能吞事件，返回 nil 即吞掉）
  - `eventsOfInterest`: `(1 << keyDown) | (1 << keyUp) | (1 << flagsChanged)`
- **FR-16.2** `.defaultTap` 需要**辅助功能权限**；`tapCreate` 返回 nil（未授权）时模块静默不装 tap，设置页引导，并每 2s 周期重试（参照 `DockClickMinimizer` 的 `installRetryTimer` 模式）。
- **FR-16.3** tap 被系统暂停（`.tapDisabledByTimeout` / `.tapDisabledByUserInput`）时重新 `CGEvent.tapEnable(tap:enable:true)`（参照 AltTab: `src/events/KeyboardEvents.swift` 的 `reEnableTapIfNeeded`）。
- **FR-16.4** 事件处理规则（回调内只做**决策 + 吞事件**，UI/抓图全部异步）。下表「配置修饰键 / 配置主键」指设置中的 ⌘⇥ 快捷键（默认 ⌘ + Tab，可改）：
  | 事件 | 状态 | 处理 |
  |---|---|---|
  | flagsChanged，配置修饰键由按下→抬起（默认即 ⌘ 抬起） | 会话活跃 | 触发「激活选中窗口」→ 吞掉该事件（return nil） |
  | flagsChanged，配置修饰键抬起 | 会话不活跃 | 放行 |
  | keyDown，配置修饰键按住 + 配置主键（默认 ⌘+Tab，kVK_Tab=48） | 任意 | 吞掉；会话不活跃→开始会话+弹面板；活跃→前进一格（按住自动重复也前进） |
  | keyDown，配置修饰键 + Shift + 配置主键（默认 ⇧⌘⇥） | 任意 | 吞掉；后退一格 |
  | keyDown，Esc(kVK_Escape=53) | 会话活跃 | 吞掉；取消会话，收起面板 |
  | keyDown，←(123)/→(124)/↑(126)/↓(125) | 会话活跃 | 吞掉；左右 ±1 格；**上下移动到相邻行里「渲染位置」（含末行居中偏移）最接近当前窗口的窗口，无相邻行则原地不动**；`selectionOffset` 与选中下标都 clamp 在边界（避免边缘越界累积导致反向失灵），tiles 未就绪时按键先累计不丢 |
  | keyDown 其他 | 会话活跃 | 放行（不做特殊处理） |
  | keyUp，配置主键 | 会话活跃 | 吞掉（避免系统收到残片） |
  | 其余 | — | 放行 |
- **FR-16.5** 自动重复：用 `keyboardEventAutorepeat` 字段识别重复的配置主键 keyDown，反复前进。
- **FR-16.6** 已知边界（记录，不强制解决）：密码框等 **Secure Input** 场景下系统会过滤 keyDown（flagsChanged 仍可达），此时可能在密码框内回退为系统切换器行为。
- **FR-16.7** 快捷键变化处理：`cmdTabShortcut` 变化 → 通知 `CommandTabSwitcher` 重新读取配置；会话进行中不打断（下一次触发生效）。

### FR-17 窗口列表 【新增】

- **FR-17.1** 用公开 API `CGWindowListCopyWindowInfo(.optionAll, kCGNullWindowID)` 枚举（`.optionAll` 含最小化/离屏窗口）。
- **FR-17.2** 提取字段：`kCGWindowNumber`、`kCGWindowOwnerPID`、`kCGWindowOwnerName`、`kCGWindowName`（标题）、`kCGWindowBounds`（JSON 解码为 CGRect）、`kCGWindowLayer`、`kCGWindowIsOnscreen`、`kCGWindowAlpha`、`kCGWindowWorkspace`。
- **FR-17.3** 过滤规则（全部满足才入列）：
  - `kCGWindowLayer == 0`（正常窗口层；Dock/菜单栏/状态栏 层值非 0 自动排除）；
  - owner 是正在运行的正常 app（`NSRunningApplication`），排除：`com.apple.dock`、`com.apple.WindowServer`、`com.apple.controlcenter`、`com.apple.systemuiserver` 等系统性进程；**不排除本 app**（`com.zeroflow.app`），否则设置窗口不会出现在切换器里（Zeroflow 的遮罩/编辑/切换面板都是高 layer，天然被过滤，只有设置窗口是 layer 0）；
  - 窗口 bounds 宽高 ≥ 2pt；
  - **保留** `kCGWindowIsOnscreen == false` 的窗口（最小化/离屏，正是系统切不到的窗口）。
  - **幽灵窗口过滤**（对齐 AltTab `PhantomWindowDetector` + `WindowDiscriminator`）：
    - 现象：alpha=0 的 Outlook 提醒、`orderOut:`/`show:false` 的 Electron 窗口、微信/Teams 隐藏窗口，以及 WindowServer 为真实窗口伴生的影子记录（如 Chrome 1272×498、微信 500×500）——存在但抓不到图、不是可切换目标。
    - **主判据（CGS/SLS 私有 API，符号缺失自动退回下面的启发式）**：
      - 可见列表 = `CGSCopyWindowsWithOptionsAndTags(..., options=1<<1)`（不含 `.invisible1/.invisible2` 标签）；全量列表 = options `1<<1|1<<0|1<<2`；
      - 每窗口 Space = `CGSCopySpacesForWindows(conn, .all)` 逐 Space 反查；当前可见 Space = `CGSCopyManagedDisplaySpaces` 的 Current Space；
      - 最小化 = SLS `tags & (1<<60)`；隐藏 app = `NSRunningApplication.isHidden`；
      - 判定顺序（对齐 AltTab `cgsVerdict`）：最小化/隐藏 app → 保留；不在全量列表 → 幽灵（强）；在可见列表 → 保留；Space 非当前可见 → 保留（其他 Space 真窗口）；否则 → 幽灵（弱）。
      - **AX subrole 兜底**：对「不可见的其他 Space 窗口」（上面「保留」分支的模糊情形）再查 AX subrole，非 `AXStandardWindow`/`AXDialog` 或取不到 AX 实体 → 幽灵（对齐 AltTab `isActualWindow`）。这能确定性剔除 Chrome 伴生记录（纯 CGS 成员判定会随当前 Space 变化而漏判）。
    - **兜底启发式**（CGS 不可用时的退化路径）：`height < 48` **或** `width/height > 6`（Chromium 伴生标题条 ≈12~15:1）**或**（标题为空 且 不在屏 且 `max(宽,高) < 400`）→ 排除。
    - 保证：真实窗口（含最小化/其他 Space/⌘H 的）有标题或尺寸足够而保留；即使标题为空，只要足够大也保留。
- **FR-17.4** 排序：**窗口级 MRU**（对齐 AltTab `lastActivityTime`）——用 `WindowActivityTracker` 监听各 app `kAXFocusedWindowChangedNotification`（`_AXUIElementGetWindow` 取 CGWindowID 打时间戳），全部窗口按最近激活时间降序；无记录按 app 级 MRU（前台 app 居前）兜底。这样 index 0 = 当前窗口、index 1 = 上一个窗口，默认选中（= 1 + offset）天然正确，同 app 多窗口也按最近使用排序。
- **FR-17.5** 标题：优先 `kCGWindowName`，为空回退 AX `kAXTitle`（如某些 app 不提供 CG 标题时，AltTab 同款 `bestEffortTitle` 思路）。
- **FR-17.6** 空间过滤：默认**列出所有 Space 的窗口**（设置可关，见 FR-21.6）。关闭后仅列当前 Space 的窗口（公开 API 下为尽力而为：以 `kCGWindowWorkspace` 与当前活跃 space 对比为主，最小化窗口若无法判定其 space 则保留）。
- **FR-17.7** 无窗口 app：开关 `windowSwitcherShowWindowlessApps`（默认**开**）开启时，**窗口全部关闭但仍运行的正常 app**（`activationPolicy == .regular` 有 Dock 图标、非排除名单、非本 app、当前列表无其窗口）显示为**占位卡（app 图标，无缩略图）**，**统一排在所有真实窗口之后**（内部按 app MRU 排序）。点击占位卡 = 激活该 app（无 AX 窗口走 `app.activate` 兜底）；悬停只显示「退出应用」按钮（关闭/最小化/全屏对无窗口 app 无意义）。
- **FR-17.8** 性能与缓存：每个会话内只枚举一次；监听 `NSWorkspace.didLaunchApplicationNotification / didTerminateApplicationNotification / NSApplication.didChangeScreenParametersNotification` 置缓存失效（参照 `DockClickMinimizer.observeDockLayoutChanges`）。

### FR-18 窗口缩略图 【新增】

- **FR-18.1** 缩略图来源：SkyLight **私有 API** `CGSHWCaptureWindowList`（与 AltTab 在 macOS 14/15 上生产做法一致：AltTab: `src/events/WindowCaptureEvents.swift` 的 `WindowCaptureScreenshotsPrivateApi.oneTimeCapture`）。它一次批处理 IPC 返回多个窗口的 `CGImage`，**且能截取最小化/离屏窗口**（公开 API ScreenCaptureKit 截不了最小化窗口）。
- **FR-18.2** 私有 API 调用方式（推荐运行时 `dlsym`，避免改工程 linker 配置）：
  ```swift
  // 函数签名（@_convention(c)）
  func CGSMainConnectionID() -> Int                // 取 WindowServer 连接
  func CGSHWCaptureWindowList(_ cid: Int,
                              _ wids: UnsafeMutablePointer<CGWindowID>,
                              _ count: Int32,
                              _ options: Int32) -> Unmanaged<CFArray>?  // 返回 [CGImage]
  // options 位值（AltTab 当前使用的 CGSWindowCaptureOptions，现代 macOS）：
  //   .ignoreGlobalClipShape = 1 << 11 (0x800)  跳过全局裁剪（含被遮挡窗口）
  //   .bestResolution        = 1 << 8  (0x100)  最优分辨率
  //   .fullSize             = 1 << 19 (0x80000)  全尺寸
  //   组合 = 0x80900。⚠️ 旧头文件的 0x04|0x10|0x20 已失效，传 0x34 会返回空。
  ```
  `dlsym` 加载 `/System/Library/PrivateFrameworks/SkyLight.framework/SkyLight`，`unsafeBitCast` 为上述类型后调用；`takeRetainedValue()` 取 `CFArray`，`as? [CGImage]` 转数组。抓到的图按窗口卡逻辑尺寸等比缩小后再展示。
- **FR-18.3** 抓图执行：全程在**后台串行队列**，**逐窗口调用** `CGSHWCaptureWindowList`（一次只传 1 个 `CGWindowID`）。该私有 API 实际只认窗口列表里的第一个（AltTab 源码注释），批量传会丢其余窗口，故不用 ≤16 分批。
- **FR-18.4** 缓存：按 `CGWindowID` 缓存抓到的缩略图；同一会话内不再抓已缓存的窗口；会话启动时缓存已存在且 TTL 未过期（建议 TTL ≈ 5s）直接复用。会话销毁时可清空对象引用（内存只随会话存活）。
- **FR-18.5** 节流：对单窗口不重复抓取（≥800ms 内不重复抓同窗口，参照 AltTab `screenshotThrottler`）。会话打开瞬间避免全量高并发抓图导致系统截图阻塞。
- **FR-18.6** 占位与降级：抓图完成前面板显示 app 图标（`NSRunningApplication.icon`）占位；抓取失败/被系统拒绝的窗口保留图标卡，不弹错不崩溃。
- **FR-18.7** 权限依赖：
  - 有屏幕录制权限：缩略图真实、窗口标题齐全；
  - 无屏幕录制权限：缩略图回退 app 图标、标题用 app 名（AX 标题需辅助功能权限），**切换功能仍可用**（AX 激活不依赖屏幕录制）。
- **FR-18.8** 若未来 macOS 移除/改名该私有 API：全部抓图失败 → 自动降级为图标卡，不影响列表与切换；记录日志（`ZSLog`）。

### FR-19 切换 / 激活窗口 【新增】

- **FR-19.1** 最小化窗口先还原：`AXUIElementSetAttributeValue(axWindow, kAXMinimizedAttribute, kCFBooleanFalse)`（AltTab: `src/switcher/state/Window.swift` focus() 第 397-399 行同款）。还原动画结束前（约 0.4–0.7s）不对该窗口重新抓图（参照 AltTab `WindowThumbnails.deferCaptureUntilRestoreEnds` 思路，避免抓到迷你帧）。
- **FR-19.2** 激活 app：`app.activate(options: [.activateAllWindows, .activateIgnoringOtherApps])`。注意 macOS 14 起该 API 降级为「建议性请求」，不能单独依赖，必须叠加下面 AX 步骤。
- **FR-19.3** AX 精确定位目标窗口：
  1. `AXUIElementCreateApplication(pid)` → 取 `kAXWindowsAttribute` 枚举窗口；
  2. 匹配 CGWindowID（二选一）：
     - 私有单行桥 `_AXUIElementGetWindow(axWindow, &wid)`（`@_silgen_name("_AXUIElementGetWindow")` 声明，链接 `ApplicationServices.framework`；AltTab: `src/macos/api-wrappers/HIServices.framework.swift`）——**推荐**；
     - 纯公开兜底：以 AX 窗口 `kAXPosition`+`kAXSize` 与 CGWindow bounds 匹配（AltTab: `src/switcher/state/BruteForceWindowMatch.swift` 曾用思路）。
  3. 置顶：`AXUIElementSetAttributeValue(axWindow, kAXMainAttribute, kCFBooleanTrue)` + `AXUIElementPerformAction(axWindow, kAXRaiseAction)`。
- **FR-19.4** 跨 Space：目标窗口在其他 Space 时，上述 activate + raise 通常会让 macOS 自动切换 Space（AltTab 本节有额外加固，v1 不做：`_SLPSSetFrontProcessWithOptions`/`SLSSpaceSetFrontPSN` 等私有进程管理接口）。
- **FR-19.5** 无窗口 app 占位卡：`app.activate` 对无窗口 app 通常无效，**先 `NSWorkspace.shared.launchApplication(at: bundleURL)` 重新启动置前**（多数 app 会尝试重开窗口），失败退回 `app.activate`（对齐 AltTab `Window.focus` 的 windowless 分支）。
- **FR-19.6** 线程：AX 写操作放到专门的后台串行队列执行，绝不在事件 tap 回调里跑 AX（回调只做决策+吞事件，与 Dock 模块同一原则）。
- **FR-19.7** 本 app 窗口特例：目标是 Zeroflow 自己的窗口时**不走 AX**——AX 在本进程内会被 AppKit 转成 `[NSWindow makeKeyAndOrderFront:]`，后台线程调用会触发 AppKit 崩溃（SIGTRAP，实测崩溃日志）。改为主线程：`NSApp.activate(ignoringOtherApps:)` + 按 `windowNumber == Int(wid)` 找 NSWindow 直接 `makeKeyAndOrderFront(nil)`（最小化先 `deminiaturize`）。
- **FR-19.8** `_AXUIElementGetWindow` 不可用时（策略顾虑）：走 FR-19.3 的公开兜底匹配，功能不依赖任何私有 AX 桥也可工作。

### FR-20 缩略图面板 【新增】

- **FR-20.1** 面板窗口 `NSPanel`，配置与现有截图遮罩窗口 `CaptureOverlayWindow`（`Zeroflow/Views/CaptureOverlayView.swift`）一致：
  - `styleMask: [.borderless, .nonactivatingPanel]`；`level = .screenSaver`；
  - `isOpaque = false`、`backgroundColor = .clear`、`hasShadow = true`；
  - `collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary, .stationary]`；
  - `isReleasedWhenClosed = false`；
  - `canBecomeKey = false`、`canBecomeMain = false`（面板永不夺走原 app 的 key/main，否则会关闭用户现有弹出框——与遮罩窗口同样的理由）。
- **FR-20.2** 键事件：面板不接收键盘（canBecomeKey=false），前进/后退/取消/激活全部由 FR-16 的事件 tap 处理；鼠标悬停/点击用 SwiftUI 手势处理。
- **FR-20.3** 内容（SwiftUI `NSHostingView` 承载）：
  - 手动分行网格（每行最多 **6 个**卡片，卡片 ≈ 168×118pt 逻辑尺寸）展示窗口卡；窗口**宽度随窗口数量自适应**（不足 6 个时按实际列数收窄，超过则换行）；**不满的一行内容居中**（每行 `HStack(spacing:12) + frame(maxWidth:.infinity)`），网格整体纵向滚动：
    - 选中态：高亮描边（如蓝色 2pt 圆角边框）+ 轻微放大；
    - 卡片主体：缩略图（未就绪显示 app 图标占位）；
    - 卡片底部：窗口标题（单行截断）＋ 小号 app 名。
  - 悬停：`onHover` 改变高亮底色 + 显示该卡片左上角的操作按钮（**不移动选择**，选择只由键盘 Tab/方向键控制）；点击：立即切换该窗口。
- **FR-20.4** 定位：面板居中于**鼠标当前所在屏幕**（`NSScreen.screens.first{ $0.frame.contains(NSEvent.mouseLocation) }`），多屏适配。
- **FR-20.5** 打开/关闭：**先抓缩略图、就绪后再 `orderFront`**（避免「先 app 图标后缩略图」的闪烁；250ms 兜底计时器防止抓图过慢时面板迟迟不出现，未就绪瓦片仍显示 app 图标占位）。`orderOut` 收起；会话结束（激活或取消）时销毁面板内容状态。
- **FR-20.6** 卡片操作按钮（对齐 AltTab WindowOps）：
  - 悬停卡片时**左上角**（贴卡顶边，`alignment: .topLeading`）显示 4 个圆形按钮，**系统红绿灯样式（直径 12pt，与 macOS 一致）**：`power`(退出应用，紫) / `xmark`(关闭，红) / `minus`(最小化，黄，符号用黑) / `arrow.up.left.and.arrow.down.right`(全屏切换，绿)；按钮自带 tap 手势，优先于整卡切换，点击不触发选中激活，会话保持打开。
  - 执行（`WindowOps`）：
    - **本 app 窗口 → 主线程 NSWindow**：退出=`NSApp.terminate`、关闭=`performClose`、最小化=`miniaturize`、全屏=`toggleFullScreen`（绕开 AX 后台线程操作本进程窗口的崩溃，同 FR-19.7）；
    - **其他 app → 后台 AX**：退出=`app.terminate()`、关闭=**按 AX 关闭按钮（`AXCloseButton` + `AXPress`，即系统红绿灯路径；多数 app 不实现窗口的 `AXClose` action）**、最小化=`kAXMinimized=true`、全屏=`AXFullScreen` 取反写入。
  - 操作完成后从面板移除受影响窗口（退出应用移除该 pid 全部窗口），clamp 选中；列表空则收起面板。
  - AX 窗口匹配复用 `AXWindow.element`（`_AXUIElementGetWindow` + bounds 兜底），与激活共用。

### FR-21 设置页「切换」标签页 【新增】

- **FR-21.1** `SettingsTab` 枚举新增 `case switcher = "切换"`（放在「截图」与「Dock」之间）。标签页按项目现有 `Form` + `.formStyle(.grouped)` 风格实现。
- **FR-21.2** 顶部 Section「启用」：
  - `Toggle("用 ⌘⇥ 显示窗口缩略图", isOn: $store.cmdTabSwitcherEnabled)`；
  - footer：`"开启后，按住 ⌘ 再按 ⇥ 会弹出窗口缩略图切换器，可切换到最小化、同 app 多窗口、其他 Space 的窗口。关闭后完全恢复系统默认。"`
- **FR-21.3** 开关打开时（`cmdTabSwitcherEnabled` 变化触发，参照截图总开关的 `onChange` 写法）：
  - 若未授权辅助功能权限 → `AccessibilityPermission.requestAuthorization()`。
- **FR-21.4** 快捷键 Section（开关开启后显示）：
  - 固定两选一单选框，不提供自由录制／自定义（见 FR-15.6/15.7）：
    ```
    Section {
        Picker("切换快捷键", selection: $store.cmdTabShortcut) {
            Text("⌘⇥").tag(ShortcutKey.cmdTabDefault)
            Text("⌥`").tag(ShortcutKey.optionGraveDefault)
        }
        .pickerStyle(.radioGroup)
    }
    ```
  - 直接把 `cmdTabShortcut` 设为两个固定 `ShortcutKey` 之一（`ShortcutKey: Hashable`），didSet 通知走 `CommandTabSwitcher.shortcutDidChangeNotification`，下一次触发即生效；
  - footer 说明：`"选择其一弹出切换器：⌘⇥（Command + Tab）或 ⌥`（Option + Tab 上方的「`」键）。⇧ + 修饰键 + 主键 后退。"`
- **FR-21.5** 权限引导 Section（开关开启后显示；**独立按钮**，不直接复用/联动 Dock 模块的行，即使两者是同一个辅助功能权限）：
  - 样式与 `DockPermissionRow` 完全一致，制作为**可复用组件** `AccessibilityPermissionRow`（把现有 `DockPermissionRow` 抽成公共组件，Dock 标签页也改用它，避免两套实现漂移）：
    - 已授权 → 绿色 `checkmark.circle.fill`「辅助功能权限已开启」；
    - 未授权 → 橙色 `exclamationmark.triangle.fill`「需要「辅助功能」权限才能接管 ⌘⇥ 切换」+ 两个按钮「打开系统设置授权」「重新检测」；
    - 授权后回到 app 自动重查（`NSApplication.didBecomeActiveNotification`）。
  - 同 Section 下方补充说明文字：「该权限与 Dock 单击最小化共用，若已开启则此处直接显示绿色。」
- **FR-21.6** 选项 Section（开关开启后显示）：
  - `Toggle("显示其他 Space 的窗口", isOn: $store.windowSwitcherAllSpaces)`，默认**开**；
  - `Toggle("显示无窗口的应用", isOn: $store.windowSwitcherShowWindowlessApps)`，默认**开**（见 FR-17.7）；
  - footer：`"关闭后只显示当前 Space 的窗口。"`
- **FR-21.7** 屏幕录制权限（开关开启后显示）：复用截图页的 `ScreenRecordingPermissionRow()`（实时状态：绿色=已授权；橙色=引导授权 + 重新检测）。无权限时缩略图回退 app 图标、但切换仍可用（见 FR-18.7）。

### FR-22 权限汇总 【新增】

| 权限 | 用途 | 无权限时表现 |
|---|---|---|
| 辅助功能（`AXIsProcessTrusted`） | 事件 tap 吞事件 + AX 激活窗口 | 模块静默不生效，设置页橙色引导；授权后自动生效 |
| 屏幕录制（`CGPreflightScreenCaptureAccess`） | 窗口标题 + 真实缩略图 | 缩略图回退 app 图标、标题回退 app 名，切换仍可用 |

### FR-23 忽略名单 【预留，第一版不做】

- 预留「忽略的 app」名单设置（key：`cmdTabSwitcherIgnoredApps: [String]`，存 bundleID），被列入的 app 不出现在面板、不可被切到。与 AltTab 的 ExceptionsTab 对齐（AltTab: `src/switcher/state/ExceptionMatcher.swift`）。第一版不实现，仅保证架构不排斥后续加（WindowList 过滤处留扩展点）。

---

## 4. 设置持久化（UserDefaults Key）

| Key | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `cmdTabSwitcherEnabled` | Bool | false | 窗口切换器总开关 |
| `windowSwitcherAllSpaces` | Bool | true | 列出其他 Space 的窗口 |
| `windowSwitcherShowWindowlessApps` | Bool | true | 显示无窗口但仍在运行的应用（占位卡，排最后） |
| `cmdTabShortcut.keyCode` | Int | 48 (tab) | 切换快捷键主键键码；固定两选一：48=Tab（⌘⇥）/ 50=「`」键（⌥`，kVK_ANSI_Grave） |
| `cmdTabShortcut.modifiers` | Int | 1048576 | 切换快捷键修饰键（⌘⇥=`.command` = `1 << 20`；⌥`=`.option` = `1 << 19`） |
| `cmdTabSwitcherIgnoredApps` | [String] | [] | 忽略名单（bundleID，FR-23 预留，第一版不写不读） |

> 需同步追加到主文档 `需求文档.md` 的 FR-12.9 表。

---

## 5. 架构与文件清单

### 5.1 新增文件（工程用 `PBXFileSystemSynchronizedRootGroup`，新 .swift 自动纳入编译，无需改 pbxproj）

| 文件 | 职责 | 对应 AltTab 参考 |
|---|---|---|
| `Zeroflow/Services/CommandTabSwitcher.swift` | 事件 tap + 会话状态机 + 启停/重试（结构仿 `DockClickMinimizer`） | `src/events/KeyboardEvents.swift`、`src/switcher/SwitcherSession.swift` |
| `Zeroflow/Services/WindowList.swift` | 窗口枚举 + 幽灵过滤 + 窗口级 MRU 排序 | `src/windowserver/WindowServerQuery.swift`、`src/switcher/state/Window.swift` |
| `Zeroflow/Services/CGSWindowServer.swift` | SkyLight/CGS 私有 API dlsym 桥（SLS 批量枚举 + 成员列表 + Space 拓扑），带公开 API 兜底 | `src/macos/CGSCallScheduler.swift`、`src/macos/api-wrappers/SkyLight.framework.swift` |
| `Zeroflow/Services/PhantomWindowDetector.swift` | 幽灵窗口判定纯函数（对齐 AltTab cgsVerdict） | `src/switcher/state/PhantomWindowDetector.swift` |
| `Zeroflow/Services/WindowActivityTracker.swift` | 窗口级 MRU：AX `kAXFocusedWindowChangedNotification` 焦点观察 | `src/windowserver/ActivationFocusResolver.swift` 等 lastActivityTime 机制 |
| `Zeroflow/Services/WindowThumbnailer.swift` | SkyLight `dlsym` 桥接 + 后台抓图 + 缓存/节流/降级 | `src/events/WindowCaptureEvents.swift`（`WindowCaptureScreenshotsPrivateApi`）、`src/switcher/state/WindowThumbnails.swift` |
| `Zeroflow/Services/WindowActivator.swift` | AX 还原/前置/激活/匹配窗口 + 本 app 主线程激活 | `src/switcher/state/Window.swift` focus() |
| `Zeroflow/Services/AXWindow.swift` | 共享 CGWindowID→AXUIElement 匹配（`_AXUIElementGetWindow` + bounds 兜底） + subrole 读取 | `src/windowserver/WindowElementAcquisition.swift` |
| `Zeroflow/Services/WindowOps.swift` | 卡片操作：退出/关闭/最小化/全屏（本 app 主线程 NSWindow、其他 app 后台 AX） | `src/state/WindowOps.swift` |
| `Zeroflow/Views/WindowSwitcherPanel.swift` | NSPanel + SwiftUI 瓦片网格（含 `WindowSwitcherPanelView`） | `src/switcher/main-window/TilesPanel.swift`、`TilesView.swift`（简化版） |
| `Zeroflow/Views/AccessibilityPermissionRow.swift` | 由 `DockPermissionRow` 抽出的公共辅助功能权限行 | —（本仓库内复用 `Zeroflow/Views/SettingsView.swift`） |

### 5.2 修改文件

| 文件 | 改动 |
|---|---|
| `Zeroflow/Models/SettingsStore.swift` | 新增 `cmdTabSwitcherEnabled`、`windowSwitcherAllSpaces`、`windowSwitcherShowWindowlessApps`、`cmdTabShortcut`（Key + `@Published` + defaults 注册 + 通知：开关变化发 `CommandTabSwitcher.didChangeNotification`、快捷键变化发 `CommandTabSwitcher.shortcutDidChangeNotification`；加载时把非两选一旧值钳回默认） |
| `Zeroflow/Views/ShortcutRecorderView.swift` | 泛化：注入 `Binding<ShortcutKey>` + `onSuspend/onResume` 闭包；**仅截图页使用**（「切换」页改为固定 Picker 二选一，见 FR-21.4） |
| `Zeroflow/Views/SettingsView.swift` | `SettingsTab` 加 `.switcher`；新增 `switcherTab`；`DockPermissionRow` 改用公共 `AccessibilityPermissionRow` |
| `Zeroflow/MenuBar/MenuBarController.swift` | `applicationDidFinishLaunching` 中调用 `CommandTabSwitcher.shared.reapply()` |
| `AGENTS.md` | 补充模块一句话说明 + 调试环境变量表加 `ZEROFLOW_SWITCHER_DEBUG=1` |
| `需求文档.md` / `README.md` | FR-12.9 Key 表、新增 FR 章节、README 功能列表 |

### 5.3 启动/接线时序

```
MenuBarController.applicationDidFinishLaunching
  └─ CommandTabSwitcher.shared.reapply()
       ├─ 读取 cmdTabSwitcherEnabled + cmdTabShortcut
       ├─ ON  → 后台线程装 tap（2s 周期重试直至授权）
       └─ OFF → 卸 tap
CommandTabSwitcher.didChangeNotification（开关联动）
  └─ reapply()
CommandTabSwitcher.shortcutDidChangeNotification（快捷键联动）
  └─ 会话未激活时重读快捷键；会话激活时不打断（下次生效）
```

### 5.4 调试辅助环境变量

| 变量 | 作用 |
|---|---|
| `ZEROFLOW_SWITCHER_DEBUG=1` | 启动后 dump 一次窗口列表（数量、app、是否含最小化/其他 space 窗口）到 ZSLog，并在日志打印一次「⌘⇥ tap 是否已安装」|

---

## 6. 边界场景与异常处理

1. **未授权辅助功能** → tap 不装、模块静默、设置页橙色引导；授权返回自动生效，无需重启。
2. **未授权屏幕录制** → 面板仍可用，缩略图回退 app 图标、标题回退 app 名。
3. **最小化窗口** → 列表中保留；缩略图走私有 API 可抓；选中后先 AX 还原再激活；还原动画期不重抓。
4. **其他 Space 窗口** → 默认列出；选中后由 activate+raise 自动切 Space。
5. **Secure Input（密码框）** → keyDown tap 被系统过滤，可能回退系统切换（已知限制，不强制解决）。
6. **私有 SkyLight API 失效** → 抓图全部失败时降级为图标卡；列表/切换不受影响；`ZSLog` 记录。
7. **tap 被系统暂停/超时** → 重新 `tapEnable` 恢复；tap 创建失败每 2s 重试。
8. **与截图/Dock 模块共存** → 三套事件机制互不冲突；会话互斥只在本模块内（会话进行中重复 ⌘⇥ 视为前进）。
9. **面板打开时 app 被关闭/结束** → 目标 app 退出则丢弃该会话（`applicationWillTerminate` 时清理 tap，参照 AltTab `applicationWillTerminate` 还原系统热键的精神，本模块 tap 随进程退出自然消失）。

---

## 7. 验收标准

1. 设置「切换」标签页有独立开关 + 权限引导按钮（绿/橙两态正确）+ 快捷键录制器（默认显示 `⌘⇥`）。
2. 开启后，在任意 app 按 ⌘⇥：出现缩略图面板，**系统切换器完全不出现**。
3. ⇥ 前进 / ⇧⇥ 后退 / 按住连发 / Esc 取消正确；松开 ⌘ 立即切到选中的窗口。
4. 把快捷键改为 ⌥⇥ 并保存 → 立即生效：按 ⌥⇥ 弹面板、⌥⇥/⌥⇧⇥ 前后退、松 ⌥ 切换；⌘⇥ 恢复系统默认行为；「恢复默认」可回到 ⌘⇥。
5. 同 app 开 2 个窗口、最小化 1 个窗口、⌘H 一个 app、其他 Space 放 1 个窗口 → 均出现在面板并可被切到（最小化窗口切后自动还原）；无窗口后台 app 不出现（与 AltTab 默认一致）。
6. 缩略图真实显示各窗口画面；最小化窗口也有缩略图；个别窗口抓不到时显示 app 图标。
7. 鼠标悬停高亮、单击即切换；多屏下面板居中于鼠标所在屏。
8. 关闭总开关 → tap 移除、系统 ⌘⇥ 完全恢复、系列行为如常。
9. 无辅助功能权限时：开关可打开、模块静默、仅设置页引导，不崩溃。
10. `xcodebuild` Debug 构建通过；`ZEROFLOW_SWITCHER_DEBUG=1` 日志可打。

---

## 8. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| S1 基础 | 数据模型/设置项/新标签页/事件 tap + 面板骨架 | 未开始 |
| S2 列表与缩略图 | WindowList + WindowThumbnailer（SkyLight）+ 缓存降级 | 未开始 |
| S3 切换与打磨 | WindowActivator + 悬停点击 + 多屏 + 调试探针 + 文档 | 未开始 |

---

## 9. 附录：AltTab 具体引用清单（实现时对照）

| 目的 | AltTab 文件 |
|---|---|
| 事件 tap 创建、tap 失效恢复、Esc 吸收思路 | `src/events/KeyboardEvents.swift` |
| 会话生命周期（`SwitcherSession`） | `src/switcher/SwitcherSession.swift` |
| 窗口列表采集（我方可简化为公开 API） | `src/windowserver/WindowServerQuery.swift`、`src/windowserver/WsWindowState.swift` |
| 缩略图抓取（私有 API `CGSHWCaptureWindowList`） | `src/events/WindowCaptureEvents.swift` → `WindowCaptureScreenshotsPrivateApi` |
| 缩略图尺寸/节流/恢复动画延迟 | `src/switcher/state/WindowThumbnails.swift` |
| 激活/前置/最小化还原 | `src/switcher/state/Window.swift` → `func focus()` |
| 私有单行桥 `_AXUIElementGetWindow` 等 | `src/macos/api-wrappers/HIServices.framework.swift` |
| AX↔wid 匹配兜底 | `src/switcher/state/BruteForceWindowMatch.swift` |
| 面板 UI | `src/switcher/main-window/TilesPanel.swift`、`src/switcher/main-window/TilesView.swift` |