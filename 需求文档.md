# zeroflow — macOS 原生截屏软件需求文档

> 版本：v1.0  
> 状态：已评审，可进入开发（MVP）

---

## 1. 项目概述

### 1.1 产品定位
一款**极简、轻量、启动迅速**的 macOS 原生截图工具。用户按快捷键即可随时截图，截图后可对图片进行轻量标注编辑（画线、文字气泡标注），并支持一键下载到本地或复制到剪贴板。

### 1.2 产品名称
zeroflow

### 1.3 技术栈
| 项 | 选型 |
|---|---|
| 语言 | Swift |
| UI 框架 | SwiftUI |
| 系统能力 | AppKit（屏幕捕获 `CGWindowListCreateImage` / `ScreenCaptureKit`）、Core Graphics（图像合成）、NSColorPanel（颜色选择） |
| 最小系统 | macOS 14.0（Sonoma）及以上（截图依赖 ScreenCaptureKit 的 SCScreenshotManager） |
| 架构 | 单工程，无第三方依赖，保持轻量 |

### 1.4 设计目标（非功能需求，详见第 6 章）
- **轻量**：安装包 ≤ 5MB，常驻内存 ≤ 80MB
- **启动迅速**：冷启动到菜单栏就绪 ≤ 0.5s；从按快捷键到进入选区模式 ≤ 0.3s
- 无侵入：常驻时无 Dock 图标（`LSUIElement = YES`），仅菜单栏图标
- 完全本地运行：无广告、无登录、无联网

### 1.5 目标用户
需要快捷截图并进行简单标注的 macOS 用户，偏好极简体验。

### 1.6 术语定义
| 术语 | 含义 |
|---|---|
| 选区模式 | 屏幕被遮罩覆盖、鼠标为十字光标、可拖拽框选的状态 |
| 编辑页 | 截图完成后弹出的编辑窗口，含图片展示与工具栏 |
| 气泡标注 | 带小尾巴的对话气泡样式文本框，指向用户点击的位置 |
| 驻留 | App 最小化到菜单栏、无 Dock 图标常驻运行的状态 |

---

## 2. 整体工作流程

```
开机 → App 自动启动（进程常驻，仅菜单栏图标）
     │
     ├─ 用户按快捷键  ⌘⇧S  (默认)
     │     ↓
     │  进入截屏模式：鼠标变为十字光标、屏幕罩上遮罩层
     │     ↓
     │  用户按下鼠标左键拖拽选区（显示尺寸提示）
     │     ↓
     │  松开鼠标 → 无缝进入【编辑页】窗口
     │     ↓
     │  展示截图 + 底部编辑工具栏（6 按钮）
     │     ├─ 用户编辑：画线 / 文字气泡标注
     │     ├─ 撤销 / 重做
     │     └─ 下载到本地 / 复制到剪贴板 → 关闭窗口，回到菜单栏驻留
     │
     └─ 任何时候可通过菜单栏打开【设置页】配置快捷键、开机自启、下载路径
```

---

## 3. 功能需求

> 优先级：P0 = 必做（MVP），P1 = 第二优先级，P2 = 可选。

### FR-1 全局快捷键唤起截图 【P0】

- **FR-1.1** 默认快捷键：`Shift + Command + S`（`⌘⇧S`）。
- **FR-1.2** 使用 macOS 全局热键注册，当 App 在前台/后台、任意其他 App 活动时均能触发。
- **FR-1.3** 同一时刻只能有一个截屏会话：若已处于选区模式或编辑状态，再次触发快捷键时忽略。
- **FR-1.4** 触发时聚焦鼠标当前所在的显示器进行截屏。
- **验收**：任意应用中按下 `⌘⇧S`，在 0.3s 内出现遮罩与十字光标。

### FR-2 选区交互 【P0】
- **FR-2.1** 遮罩层：覆盖整个屏幕（含菜单栏、Dock），半透明黑色（不透明度约 0.3）。
- **FR-2.2** 光标：进入截屏模式的瞬间，光标变为十字准星。
- **FR-2.3** 拖拽选择：按住鼠标左键拖拽形成矩形选区；选区外区域加深遮罩、选区内显示原图。
- **FR-2.4** 尺寸提示：拖拽过程中，选区边缘实时显示 `宽x高`（如 `1230×680`），跟随鼠标位置。
- **FR-2.5** Esc 取消：按 `Esc` 取消本次截图，回到菜单栏驻留，无残留遮罩。
- **FR-2.6** 高 DPI：Retina 屏幕按物理像素（2×）截取与保存，尺寸提示使用物理像素。
- **FR-2.7** 原地单击（未拖拽）视为截取全屏。
- **FR-2.8** 遮罩使用无边框全屏透明窗口（`NSPanel`，`windowLevel = .screenSaver`），确保覆盖所有窗口。
- **验收**：在单屏/多屏、系统缩放 100% / 125% / 200% 下选区坐标换算正确，尺寸提示与实际截图像素一致。

### FR-3 编辑页窗口 【P0】
- **FR-3.1** 选区完成后立即弹出编辑窗口（≤100ms 内可见）。
- **FR-3.2** 窗口构成：中间是截取的原始图片（可拖动），下方一条工具栏。
- **FR-3.3** 编辑页置顶（`nonactivatingPanel` 风格），随时可关闭（`Esc`、关闭按钮、完成保存/复制后自动关闭）。
- **FR-3.4** 窗口标题栏/背景：极简深色或跟随系统外观。

### FR-4 工具栏总览 【P0】
- **FR-4.1** 位置：编辑页底部水平居中。
- **FR-4.2** 共 6 个按钮，固定顺序（从左到右）：撤回 → 前进 → 画线 → 标注 → 下载 → 复制。
- **FR-4.3** 每个按钮使用极简线性图标（SF Symbols）。
- **FR-4.4** 鼠标悬停时显示按钮名称（tooltip，含快捷键提示），见下表。
- **FR-4.5** 全部操作均有快捷键，且仅当编辑页窗口获得焦点时生效。

| 顺序 | 按钮 | 图标（SF Symbols 参考） | 悬停提示 | 快捷键 |
|---|---|---|---|---|
| 1 | 撤回 | `arrow.uturn.backward` | 撤消（⌘Z） | `⌘Z` |
| 2 | 前进 | `arrow.uturn.forward` | 重做（⇧⌘Z） | `⇧⌘Z` |
| 3 | 画线 | `scribble.variable` / `pencil` | 画线（L） | `L` |
| 4 | 标注 | `bubble.left.and.text.bubble.right` | 标注（T） | `T` |
| 5 | 下载 | `arrow.down.to.line` | 下载保存（⌘S） | `⌘S` |
| 6 | 复制 | `doc.on.doc` | 复制（⌘C） | `⌘C` |

### FR-5 撤回 / 重做 【P0】
- **FR-5.1** 撤回最近一步可逆操作（画线的创建/移动/改色、气泡的添加/移动/改字/删除等）。
- **FR-5.2** 无可撤销操作时「撤回」按钮置灰禁用；可撤销时高亮可用。
- **FR-5.3** 重做：撤销后即时可重做；一旦发生新的编辑操作，重做栈清空。
- **FR-5.4** 历史栈上限 50 步，超出丢弃最早步骤。
- **FR-5.5** 支持工具栏按钮与键盘快捷键两种触发方式。
- **验收**：连续执行画线+气泡+移动共 10 次操作后多次撤销再重做，最终画面与操作前像素一致。

### FR-6 画线 【P0】
- **FR-6.1** 点击「画线」或按 `L` 进入画线模式，光标变为笔刷样式。
- **FR-6.2** 按住鼠标左键在图上自由绘制（即绘制贝塞尔路径并实时渲染）。
- **FR-6.3** 松开左键结束本条线，绘制结果成为独立、可选中的对象（可用鼠标选中后移动或删除）。
- **FR-6.4** 进入画线模式时，工具栏上方浮动显示线条属性面板：
  - **颜色**：预设色板（黑/白/红/橙/黄/绿/蓝/紫）+「自定义」按钮调起系统取色器。
  - **粗细**：滑块 1~10px（默认 3px），提供常用预设档（1/3/5/8px）。
- **FR-6.5** 线条平滑、抗锯齿；绘制过程中实时更新渲染。
- **FR-6.6** 按 `Esc` 退出画线模式。
- **验收**：选择红色、粗细 5px 后绘制，图片中线条颜色/粗细与预设完全一致，且可撤销。

### FR-7 文字气泡标注 【P0】
- **FR-7.1** 点击「标注」或按 `T` 进入标注模式，光标变为插入点样式。
- **FR-7.2** 点击图片任意位置，生成一个气泡式文本框：圆角矩形底、带一条指向点击位置的“小尾巴”，气泡内有闪烁光标等待输入。
- **FR-7.3** 用户可直接键入文字；文本框自动换行，最大宽度约 240px，高度自适应。
- **FR-7.4** 气泡编辑交互：
  - 点击气泡重新进入文字编辑状态；
  - 拖拽气泡可移动位置，尾巴自动旋转指向最近锚点；
  - mvp 阶段支持气泡颜色调整，文字字号使用默认值（第二版本扩展字号设置）。
- **FR-7.5** 输入完成：点击气泡外任意位置或按 `Esc` 结束本次输入。
- **FR-7.6** 气泡是独立图层对象，可被单条撤销（`⌘Z`）。
- **验收**：在图片中部点击并输入“注意这里”，生成带尾巴的气泡，且可移动、可重新编辑、可被撤销。

### FR-8 下载保存 【P0】
- **FR-8.1** 点击「下载」或按 `⌘S`，将**当前编辑后的完整图片**保存到本地。
- **FR-8.2** 保存行为受设置控制，二选一：
  - 模式 A（默认）：直接保存到「设置中配置的默认目录」。
  - 模式 B：若设置开启「每次询问保存位置」，弹出系统保存面板（`NSSavePanel`）供用户选择路径与文件名。
- **FR-8.3** 文件名规则：`zeroflow_yyyyMMdd_HHmmss.png`（如 `zeroflow_20260808_153012.png`）；同目录重名自动追加序号避免覆盖。
- **FR-8.4** 保存格式：PNG（无损）。MVP 不支持其他格式导出，接口预留扩展。
- **FR-8.5** 保存成功后在编辑页右下角出现 Toast「已保存到 /路径/文件名.png」，1.8s 后自动消失。
- **FR-8.6** 保存成功后自动关闭编辑页，回到菜单栏驻留。
- **FR-8.7** 权限：保存到用户目录（默认 `~/Downloads/zeroflow`，与设置页 FR-10.3.2 的默认值一致）无需额外权限；保存到系统受保护目录时通过标准 `NSSavePanel` 走系统权限流程。
- **验收**：默认模式下保存到配置的目录；开启询问后每次弹出面板；连续保存文件名不重复。

### FR-9 复制到剪贴板 【P0】
- **FR-9.1** 点击「复制」或按 `⌘C`，将**编辑后的完整图片**（PNG 数据）写入系统剪贴板（`NSPasteboard`，类型 `png`）。
- **FR-9.2** 复制结果可粘贴到任意支持图片的 App（微信、iMessage、Pages、预览等）。
- **FR-9.3** 成功后显示 Toast「已复制到剪贴板」并自动关闭编辑页。
- **验收**：复制后直接打开「预览」粘贴可得到完整编辑后的图片。

### FR-10 设置页 【P0】

设置页是本软件独立的配置中心，所有配置项分为若干分组，分组内使用统一的表单控件（开关 / 按键录制区 / 路径选择行）。配置即时生效、持久化保存。

**设置页总体要求**
- **FR-10.0.1** 入口：菜单栏图标 →「设置…」（`⌘,`）；重复打开只聚焦已存在的窗口，不重复创建。
- **FR-10.0.2** 窗口规格：固定尺寸（约 420×480 pt），可调整内容行间距，无缩放；居中显示于主屏。
- **FR-10.0.3** 所有设置修改后**立即生效并即时持久化**（无需「保存」按钮，也无需重启）。
- **FR-10.0.4** 持久化：使用 `UserDefaults`（`@AppStorage`）存储；存储的 key 见各分组。
- **FR-10.0.5** 关闭窗口不丢失任何已修改项；重新打开时显示当前持久化值。
- **FR-10.0.6** 结构分层如下（顶部分段控件切换三个标签页）：
  ```
  设置
   ├── 截图
   │     ├── 截屏快捷键（按键录制框 + 恢复默认）
   │     ├── 屏幕录制权限（绿色=已授权 / 橙色警告 + 授权/重检按钮）
   │     └── 保存
   │           ├── 默认保存位置（路径行 + 选择按钮 + 「打开目录」）
   │           └── 每次询问保存位置（开关）
   ├── 开机自启
   │     └── 登录时启动（开关）
   └── Dock
         └── 单击 Dock 图标最小化窗口（开关）+ 辅助功能权限引导
  ```

#### FR-10.1 截屏快捷键设置
- **FR-10.1.1** 展示当前快捷键，形式为一个可点击的「按键录制框」，内显示形如 `⇧⌘S` 的组合。
- **FR-10.1.2** 点击按键录制框后进入「录制」状态（框高亮 + 提示「按下新快捷键…」），等待用户按键。
- **FR-10.1.3** 校验规则（录制完成后立即校验并处理）：
  - 必须包含至少一个修饰键（Command/Control/Shift/Option），否则视为非法，提示「请至少按下一个修饰键（⌘/⌃/⌥/⇧）」并继续录制。
  - 主键必须是非修饰键（字母 / 数字 / F 键 / 方向键等），仅按修饰键无主键视为无效。
  - 与当前设置值相同 → 无操作直接退出录制。
- **FR-10.1.4** 冲突检测：设置后由全局热键注册器尝试注册；若与新快捷键注册失败（被系统或其他 App 占用）：
  - 弹出提示「该快捷键可能被其他应用占用，本次设置可能无法生效」；
  - 值仍写入保存，但全局热键恢复为未生效状态；设置项旁展示 ⚠️ 与「重新启用」提示；
  - 用户可再次录制修改。
- **FR-10.1.5** 「恢复默认」按钮：一键恢复 `⌘⇧S`，重注册并清除 ⚠️ 状态。
- **FR-10.1.6** 快捷键保存的 key：`globalShortcut`（存为 `keyCode` + `modifiers`）。
- **FR-10.1.7** 修改后全局热键**立即更新**，需向 M1 起的截屏模块发出热键变更通知（NotificationCenter，后 message `shortcutDidChange`）。
- **验收**
  1. 录制 ⌥⌘X → 保存并关窗重启 App → 设置仍显示 `⌥⌘X`，按下 ⌥⌘X 可触发截屏。
  2. 输入不合规组合（仅 Ctrl）→ 被拒绝并有提示。
  3. 录入一个被系统占用的快捷键 → 有冲突提示，主 App 截屏快捷键不中断，仍可用。
  4. 点「恢复默认」→ 立刻回 `⌘⇧S`。

#### FR-10.2 开机自启
- **FR-10.2.1** 开关「开机自动启动（登录时打开）」。
- **FR-10.2.2** 默认开启；可通过 `SMAppService.mainApp`（macOS 13+）注册/注销 LaunchAgent。
- **FR-10.2.3** 开启后，之后每次登录系统自动启动本 App 并驻留菜单栏，随时可截图。
- **FR-10.2.4** 注册结果反馈：成功 / 失败给 Toast 提示（如「已设置为开机自启」/「设置失败，请检查系统「登录项」权限」）。
- **FR-10.2.5** 若需要（App 被吊销等）在菜单栏显示「当前已关闭自启」的弱提示。数据 key：`launchAtLogin`。
- **验收**：开关打开后，`systemsettings` 登录项中包含本 App；重启后自动运行。

#### FR-10.3 默认保存位置
- **FR-10.3.1** 一行显示「默认保存位置」+ 当前路径（截断显示，尾部可复制），右侧按钮：`选择…`（打开 `NSOpenPanel` 选目录）、`打开`（在 Finder 打开当前目录）。
- **FR-10.3.2** 默认值：`~/Downloads/zeroflow`；首次使用或读取缺失时自动写入并创建目录（`FileManager.createDirectory`）。
- **FR-10.3.3** 用户选择的路径写入设置（key：`saveDirectory`）；若不存在尝试创建，创建失败提示。
- **FR-10.3.4** 保存面板「收藏」中包含默认目录以加快选择。
- **验收**：默认返回 `~/Downloads/zeroflow`；选择新目录后立即更新、且下载功能使用新目录。

#### FR-10.4 保存询问策略
- **FR-10.4.1** 开关「保存时是否询问位置」。
- **FR-10.4.2** 关闭（默认）：每次保存直接存到「默认保存位置」目录名自动生成。
- **FR-10.4.3** 开启：每次保存都弹 `NSSavePanel`，默认文件名与目录为默认目录。
- **FR-10.4.4** 设置页切换后即时生效，无需重启。数据 key：`askSaveLocation`。
- **验收**：在设置页切换该开关后，立即按「下载」验证行为变化正确。

#### FR-10.6 工具栏默认属性（预留分组）
- **FR-10.6.1** 「默认画笔颜色」色板（与 FR-6 画线色板一致），选择后编辑页默认线色随之变化。
- **FR-10.6.2** 「默认画笔粗细」分段控制器（1/3/5/8px），选择后作为画线默认线宽。
- **FR-10.6.3** 数据 key：`defaultPenColor` / `defaultPenWidth`，P1 优先级，演示阶段可不做。

#### FR-10.7 快捷键冲突与全局快捷键状态显示
- **FR-10.7.1** 设置页顶部区域显示全局快捷键当前状态：`可用 / 已被占用 / 未注册`，便于定位问题。
- **FR-10.7.2** 演示（Demo）阶段不需真实注册全局热键，仅录制、校验、存储；主功能落地时接通。

#### FR-10.8 设置数据持久化（AppStore Key 汇总）
| Key | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `globalShortcut` | `keyCode`+`modifiers` | `⌘⇧S` | 全局截屏快捷键 |
| `launchAtLogin` | Bool | true | 开机自启 |
| `saveDirectory` | String | `~/Downloads/zeroflow` | 默认保存目录 |
| `askSaveLocation` | Bool | false | 保存时是否询问 |
| `defaultPenColor` | Data/Color | 红 | 默认画笔颜色（P1） |
| `defaultPenWidth` | Int | 3 | 默认线宽（P1） |

- **FR-10.8.1** 所有变更写 `UserDefaults`，读取都走统一切口 `SettingsStore`（可替换测试）。

#### FR-10.9 设置页验收标准
1. 打开一个设置窗口，关窗再开，只存在一个窗口。
2. 各开关/选择的当前值正确显示并持久化（重启 App 后仍在）。
3. 修改快捷键后立即反映到全局热键（M1）；恢复默认可用。
4. 切换保存路径与询问开关后立即生效。

### FR-11 菜单栏驻留 【P0】
- **FR-11.1** 常驻 `NSStatusItem` 菜单栏，图标为简洁取景框（SF Symbol `viewfinder` / `camera.viewfinder`）。
- **FR-11.2** 点击菜单栏图标弹出菜单：
  - `立即截图（⌘⇧S）` → 进入截屏模式
  - `打开最近截图` → 在 Finder 中打开最近保存的图片
  - `设置…（⌘,）`
  - `退出 zeroflow`
- **FR-11.3** 无 Dock 图标（`LSUIElement = YES`）。

### FR-12 系统权限指引 【P0】
- **FR-12.1** 截屏权限引导内联在设置页「截图」标签页（样式与 Dock 权限引导一致）：已授权显示绿色「屏幕录制权限已开启」，未授权显示橙色警告 + 「打开系统设置授权 / 重新检测」按钮。
- **FR-12.1a** 未授权时触发截图：不弹窗，自动打开设置窗口并切换到「截图」标签页展示权限引导。
- **FR-12.2** 自动检测权限状态，未授权时禁止进入选区模式（避免产生全黑截图）。
- **验收**：全新安装、未授权状态触发时正确引导且不崩溃。

---

## 4. 数据模型与图像处理逻辑（实现级参考）

### 4.1 文档模型
- `Document`：`CGImage`（原始截图） + `[CanvasElement]`（编辑对象列表）。

### 4.2 CanvasElement 统一类型
| 类型 | 字段 |
|---|---|
| `.stroke` | 点集 `[CGPoint]`、颜色 `Color`、线宽 `CGFloat` |
| `.annotation` | 位置、文字内容、尾部锚点、边界框、颜色 |

### 4.3 渲染
- 在原始截图上按顺序重绘所有 `CanvasElement`，合成最终 `NSImage`；
- 下载 / 复制均使用该合成图（物理像素分辨率）。

### 4.4 撤销栈
- 双栈实现（undo/redo），容量上限 50。

### 4.5 多屏坐标
- 转化为 `CGDisplay` 坐标系并使用 `CGDisplayBounds` 对齐；
- Retina 屏幕按 `backingScaleFactor` 换算物理像素。

---

## 5. 界面与交互规范

| 规范 | 说明 |
|---|---|
| 图标风格 | 线性图标（SF Symbols），区分选中/未选中状态（选中显示底部指示条） |
| 工具栏样式 | 毛玻璃/深色背景，圆角容器，高 44px |
| 悬停反馈 | 图标描边高亮 + tooltip 显示名称 |
| 交互反馈 | 全部反馈 100ms 内响应，无加载动画 |
| 窗口 | 编辑页无系统标题栏、无边框、可整体拖动 |
| 主题 | 跟随系统深色/浅色切换 |
| 语言 | 界面文本简体中文；预留 `Localized` 机制便于国际化扩展 |
| 无障碍 | 按钮提供 `accessibilityLabel`，适配高对比度与减弱动态效果 |

---

## 6. 性能与质量目标

| 指标 | 目标 |
|---|---|
| 启动时间 | 冷启动到菜单栏就绪 ≤ 0.5s；快捷键到选区 ≤ 0.3s |
| 内存占用 | 驻留 ≤ 80MB，空闲无后台轮询定时器 |
| 截图延迟 | 松开鼠标到编辑页可见 ≤ 300ms |
| 选区渲染 | 拖拽 resize 时实时渲染无卡顿（60fps 目标） |
| 稳定性 | 选区/绘制过程中不允许未捕获异常；异常记录日志 |
| 磁盘 | 除用户主动保存的文件外，无残留缓存 |

---

## 7. 边缘场景与异常处理

1. **未授权屏幕录制** → 触发 FR-12 引导，禁止截取，不崩溃。
2. **多显示器且缩放比例不同** → 选区按鼠标所在屏，尺寸换算按物理像素。
3. **选区过小（小于 1px）** → 舍弃，视为取消。
4. **快捷键被其他 App/输入法占用注册失败** → 设置页提示用户更换快捷键。
5. **保存目录不可写** → Toast 报错并提示用户选择新目录。
6. **编辑未完成再次触发截图** → 忽略本次触发，避免中断当前编辑。
7. **截屏过程中切换桌面 / 最小化** → 退出选区模式，无残留界面。

---

## 8. 兼容性

| 项 | 要求 |
|---|---|
| 系统版本 | macOS 14.0（Sonoma）及以上 |
| 显示 | 支持 Retina、缩放分辨率、多显示器 |
| 芯片 | Apple Silicon 与 Intel 均支持（通用二进制） |
| 语言 | 首版简体中文，可扩展本地化 |

---

## 9. 里程碑规划

> 开发策略：**先做可运行的 Demo（M0：仅设置页），跑通后再开发主功能（M1+）**。M0 是当前唯一排期目标，M1~M4 为后续。

| 阶段 | 内容 | 交付物 |
|---|---|---|
| **M0（Demo，优先）** | 仅设置页：菜单栏入口、设置窗口、快捷键录制校验与持久化、开机自启开关、保存路径设置、保存询问开关（无实际截屏功能） | 可双击运行、设置全部可改可存的 Demo App |
| **M1（MVP）** | 菜单栏驻留、⌘⇧S 选区、编辑页、6 按钮、撤销/画线/气泡、下载/复制、权限引导 | 可日常使用版本 |
| **M2** | 快捷键真正接入全局热键（打通 M0 存储值与 M1 截屏模块）、设置项全链路生效 | 功能闭环 |
| **M3** | 体验打磨：Toast、视觉规范、多屏/缩放深测、Notarization 公证与签名 | Release 版本 |
| **M4（可选）** | 长截图、贴图置顶、图形（箭头/矩形）、复制即播等扩展 | 视用户反馈 |

### M0（Demo）详细范围与验收

**范围**：只交付「设置页 + 菜单栏入口」，不实现任何截图/编辑/保存功能。App 启动后驻留菜单栏，可打开设置；所有设置项可修改、即时生效、持久化（重启后保留）。

**必须实现**
1. `LSUIElement = YES`，启动即驻留菜单栏，显示图标。
2. 菜单栏菜单包含「设置…」与「退出」。
3. 设置窗口含下列分组与表单：
   - **快捷键与启动**：截屏快捷键录制框（仅展示 + 录制 + 校验 + 恢复默认，不做真实全局热键）、「开机自启」开关。
   - **保存**：「默认保存位置」路径行 + 选择按钮 + 打开目录、「保存时询问位置」开关。
4. 设置写入 `UserDefaults` 且重启后还原正确。
5. 每个开关/输入项修改即时反馈，无「保存」按钮。

**不实现（M0 明确砍掉）**：截屏选区、编辑工具栏、撤销、画线、气泡、下载、复制、权限引导、全局热键真实注册。

**M0 验收**：
1. 运行 → 无 Dock 图标、菜单栏出现图标。
2. 打开设置 → 修改「开机自启」开关 → 关闭 → 退出 App → 重开 → 设置值与修改前一致。
3. 选择保存目录 → 重启保留 → 点「打开」在 Finder 打开该目录。
4. 修改快捷键并恢复默认，均正确保存与回显。
5. 全程无截屏入口，不出现崩溃。

---

## 10. 验收标准总纲

满足以下全部条件即为验收通过：
1. 默认在任何 App 中按 `⌘⇧S`，立即进入十字光标截图；选区、尺寸提示、Retina 尺寸均正确。
2. 可绘制任意颜色/粗细的线，并可在画布上进行撤销/重做而不丢失内容。
3. 可添加文字气泡并移动、重新编辑、撤销，无需重启。
4. 下载覆盖三种模式：固定默认路径 / 可配置路径 / 每次询问路径。
5. 复制后粘贴到其他 App 有效。
6. 开启开机自启后，登录后图标驻留菜单栏，快捷键可正常截图。
7. 未授权屏幕录制时有正确引导且不崩溃。

---

## 11. 发布与分发

- 应用标识：`com.zeroflow.app`（拟）。
- 首次 Release 需通过 Developer ID 签名 + Notarization，提供 DMG 与 `brew` 安装方式。
- 需求追踪：FR-x.y 编号与验收条目一一对应，便于回归测试。