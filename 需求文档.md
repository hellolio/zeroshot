# zeroflow — macOS 原生截屏软件需求文档

> 版本：v1.1
> 状态：M1/M1.5/M2 及 Dock 模块已实现，M3 体验打磨中

---

## 1. 项目概述

### 1.1 产品定位
一款**极简、轻量、启动迅速**的 macOS 原生截图工具。用户按快捷键即可随时截图，截图后可对图片进行轻量标注编辑（画线、矩形/圆框标注、文字气泡、马赛克涂抹），并支持一键下载到本地或复制到剪贴板。

### 1.2 产品名称
zeroflow（曾用名 Zeroshot）

### 1.3 技术栈
| 项 | 选型 |
|---|---|
| 语言 | Swift（SWIFT_VERSION 5.0） |
| UI 框架 | SwiftUI + AppKit |
| 屏幕捕获 | ScreenCaptureKit（`SCScreenshotManager`，macOS 14+） |
| 全局热键 | Carbon `RegisterEventHotKey` |
| 开机自启 | ServiceManagement `SMAppService.mainApp` |
| Dock 单击最小化 | CGEventTap + AXUIElement |
| 持久化 | UserDefaults（统一切口 `SettingsStore`） |
| 最小系统 | macOS 14.0（Sonoma）及以上 |
| 架构 | 单工程、无第三方依赖、无 Package |

### 1.4 设计目标（非功能需求，详见第 6 章）
- **轻量**：安装包 ≤ 5MB，常驻内存 ≤ 80MB
- **启动迅速**：冷启动到菜单栏就绪 ≤ 0.5s；从按快捷键到进入选区模式 ≤ 0.3s
- 无侵入：常驻时无 Dock 图标（`LSUIElement = YES`），仅菜单栏图标
- 完全本地运行：无广告、无登录、无联网

### 1.5 目标用户
需要快捷截图并进行简单标注的 macOS 用户，偏好极简体验。

### 1.6 术语定义
| 术语 | 含义 |
|---|---|
| 选区模式 | 屏幕被遮罩覆盖、鼠标为十字光标、可拖拽框选的状态 |
| 编辑页 | 截图完成后弹出的编辑窗口，含图片展示与工具栏 |
| 气泡标注 | 带小尾巴的对话气泡样式文本框，指向用户点击的位置（锚点） |
| 驻留 | App 常驻菜单栏、无 Dock 图标运行的状态 |

---

## 2. 整体工作流程

```
开机 → App 自动启动（进程常驻，仅菜单栏图标，无 Dock 图标）
     │
     ├─ 用户按快捷键 ⌘⇧S（默认，可自定义）或菜单栏「立即截图」
     │     ↓（截图功能总开关关闭时忽略本次触发）
     │     ↓ 未授权屏幕录制 → 自动打开设置页「截图」标签页展示权限引导
     │     ↓ 已授权 → 进入选区模式：十字光标 + 每个显示器一个全屏遮罩窗
     │     ↓ 拖拽选区（实时显示 宽x高）→ 松开 → 捕获 → 无缝进入编辑页
     │     ↓ 编辑：画线 / 矩形标注 / 圆框标注 / 文字气泡 / 马赛克 + 撤销/重做
     │     ↓ 下载到本地 / 复制到剪贴板（不自动关窗）
     │
     ├─ 设置页（通用 / 截图 / Dock 三个标签页）
     │
     └─ Dock 单击最小化模块（设置页开关开启时生效，后台无感运行）
```

---

## 3. 功能需求

> 优先级：P0 = 已实现（MVP+），P1 = 第二优先级，P2 = 可选。

### FR-1 全局快捷键唤起截图 【P0 已实现】

- **FR-1.1** 默认快捷键：`Shift + Command + S`（`⌘⇧S`）。
- **FR-1.2** 使用 Carbon 全局热键注册，当 App 在前台/后台、任意其他 App 活动时均能触发。
- **FR-1.3** 同一时刻只能有一个截屏会话：若已处于选区模式，再次触发快捷键时忽略（`CaptureCoordinator.isActive` 防重入）。
- **FR-1.4** 触发时聚焦鼠标当前所在的显示器进行截屏。
- **FR-1.5** 截图功能总开关（`screenshotEnabled`）关闭时，全局热键不注册、触发被忽略。
- **验收**：任意应用中按下 `⌘⇧S`，在 0.3s 内出现遮罩与十字光标。

### FR-2 选区交互 【P0 已实现】

- **FR-2.1** 遮罩层：每个显示器一个无边框全屏透明窗口（`NSPanel` 风格，`windowLevel = .screenSaver`，`canJoinAllSpaces`），半透明黑色遮罩（不透明度 0.35）。
- **FR-2.2** 光标：进入截屏模式的瞬间，光标变为十字准星。
- **FR-2.3** 拖拽选择：按住鼠标左键拖拽形成矩形选区；选区外加深遮罩、选区内 clear 显示原图、蓝色边框。
- **FR-2.4** 尺寸提示：拖拽过程中，选区边缘实时显示 `宽x高`（如 `1230×680`），跟随鼠标位置。
- **FR-2.5** Esc 取消：按 `Esc` 取消本次截图，回到菜单栏驻留，无残留遮罩。
- **FR-2.6** 高 DPI：Retina 屏幕按物理像素（`backingScaleFactor`）截取与保存，尺寸提示使用物理像素。
- **FR-2.7** 原地单击（拖拽 <1px）视为截取全屏；仅单边 ≥1px 的选区视为无效，按取消处理。
- **FR-2.8** 把光标放置到鼠标所在显示器对应的遮罩窗口并使其成为 key 窗口（可接收 Esc）。
- **验收**：在单屏/多屏、系统缩放 100% / 125% / 200% 下选区坐标换算正确，尺寸提示与实际截图像素一致。

### FR-3 编辑页窗口 【P0 已实现】

- **FR-3.1** 选区完成后立即弹出编辑窗口。
- **FR-3.2** 窗口构成：中间是截取的原始图片（等比居中、只缩小不放大，保证像素清晰），下方一条工具栏。
- **FR-3.3** 窗口带标题栏（标题「zeroflow 截图」）、可关闭、可缩放；初始尺寸按截图自适应（上限约 1100×760，下限 720×300 保证工具栏完整显示）。
- **FR-3.4** 关闭方式：Esc、关闭按钮、⌘W；下载/复制不自动关窗。

### FR-4 工具栏总览 【P0 已实现】

- **FR-4.1** 位置：编辑页底部水平排列。
- **FR-4.2** 按钮按用途分组（撤销/重做 → 绘制工具 → 标注 → 马赛克 → 下载/复制），固定顺序见下表。
- **FR-4.3** 按钮使用极简线性图标（SF Symbols），选中态以高亮底色/描边区分。
- **FR-4.4** 鼠标悬停时显示按钮名称 tooltip（含快捷键提示）。
- **FR-4.5** 全部操作均有快捷键（绘制类工具除外），且仅当编辑页窗口获得焦点时生效。

| 顺序 | 按钮 | 图标（SF Symbols 参考） | 悬停提示 | 快捷键 |
|---|---|---|---|---|
| 1 | 撤回 | `arrow.uturn.backward` | 撤回 (⌘Z) | `⌘Z` |
| 2 | 重做 | `arrow.uturn.forward` | 重做 (⇧⌘Z) | `⇧⌘Z` |
| 3 | 画线 | `pencil` | 画线 (⌘L) | `⌘L` |
| 4 | 矩形标注 | `rectangle` | 矩形标注 | — |
| 5 | 圆框标注 | `circle` | 圆框标注 | — |
| 6 | 画笔颜色 | 自绘色圆 | 画笔颜色 | — |
| 7 | 粗细 | 自绘线宽 | 粗细 | — |
| 8 | 标注 | `text.bubble.fill` | 标注 (T) | `T` |
| 9 | 马赛克 小/中/大 | 自绘像素图标 | 马赛克 小/中/大 | — |
| 10 | 下载 | `arrow.down.to.line` | 下载保存 (⌘S) | `⌘S` |
| 11 | 复制 | `doc.on.doc` | 复制 (⌘C) | `⌘C` |

- **FR-4.6** 画笔颜色与粗细仅在画线/矩形/圆框工具激活时可操作（其他工具下置灰）。
- **FR-4.7** 撤销/重做按钮在不可用（栈空）时置灰。

### FR-5 撤回 / 重做 【P0 已实现】

- **FR-5.1** 可撤销操作：画线、矩形/圆框、文字气泡、马赛克的创建；气泡/形状的移动、缩放、删除；气泡文字修改。
- **FR-5.2** 无可撤销操作时「撤回」按钮置灰禁用；可撤销时高亮可用。
- **FR-5.3** 重做：撤销后即时可重做；一旦发生新的编辑操作，重做栈清空。
- **FR-5.4** 历史栈上限 50 步，超出丢弃最早步骤。
- **FR-5.5** 支持工具栏按钮与键盘快捷键（`⌘Z` / `⇧⌘Z`）两种触发方式。
- **验收**：连续执行多种编辑共 10 次操作后多次撤销再重做，最终画面与操作前像素一致。

### FR-6 画线 【P0 已实现】

- **FR-6.1** 点击「画线」按钮或按 `⌘L` 进入画线模式；再次点击按钮切回选择。
- **FR-6.2** 按住鼠标左键在图上自由绘制（连续点集 + 圆角线段实时渲染）。
- **FR-6.3** 松开左键结束本条线，成为独立对象（可整体被撤销；当前版本画线不支持选中/移动/删除）。
- **FR-6.4** 颜色与粗细在工具栏内直接操作（无浮动面板）：
  - **颜色**：10 个预设色（红/橙/黄/绿/蓝/紫/粉/灰/黑/白），点击色圆弹出预设色板；默认红色。
  - **粗细**：弹出面板选 细/中/粗（2 / 5 / 9px），默认 3px。
- **FR-6.5** 线条平滑、抗锯齿；绘制过程中实时更新渲染。
- **FR-6.6** 按 `Esc` 关闭编辑页（无单独「退出画线模式」快捷键，点按钮或 `⌘L` 切换工具）。
- **验收**：选择红色、粗细 5 后绘制，线条颜色/粗细与预设一致，且可撤销。

### FR-7 矩形 / 圆框标注 【P0 已实现】

- **FR-7.1** 点击「矩形标注」/「圆框标注」按钮进入对应绘制模式。
- **FR-7.2** 拖拽生成仅描边、中间透明的矩形 / 圆框；颜色与线宽跟随画笔设置。
- **FR-7.3** 松开左键提交，成为独立可选中对象：单击选中显示四角缩放控点，可拖动移动、拖角缩放、`Backspace`/`Delete` 删除。
- **FR-7.4** 绘制完成后自动切回选择工具。

### FR-8 文字气泡标注 【P0 已实现】

- **FR-8.1** 点击「标注」按钮或按 `T`（再次按 `T` 切回选择）进入标注模式。
- **FR-8.2** 点击图片任意位置，生成一个气泡：圆角白色矩形 + 黑色文字 + 一条指向锚点的引线 + 锚点圆点，并**立即进入文字编辑**（白底文本框内闪烁光标等待输入）。
- **FR-8.3** 文本自动换行、气泡尺寸随文字自适应（宽上限 240px、高上限 320px，锚定右上角生长）。
- **FR-8.4** 气泡固定白色底 / 黑色文字（mvp 不支持改气泡颜色）。
- **FR-8.5** 气泡编辑交互：
  - **双击**气泡重新进入文字编辑；
  - 单击选中；拖拽移动（整框 + 锚点一起动）、拖四角缩放、拖锚点圆点单独移动引线起点；
  - `Backspace` / `Delete` 删除选中气泡；
  - 点击气泡外空白处或按 `Esc` 提交文字并退出编辑。
- **FR-8.6** 气泡是独立图层对象，可被单条撤销（`⌘Z`）。
- **验收**：点击并输入文字，生成带尾巴的气泡，可移动、缩放、重编辑、删除、撤销。

### FR-9 马赛克涂抹 【P0 已实现】

- **FR-9.1** 点击「马赛克 小/中/大」按钮进入马赛克模式（笔刷半径 12 / 18 / 28px）。
- **FR-9.2** 按住鼠标左键在图上涂抹，涂抹区域实时显示为马赛克（整图像素化后按圆形笔刷裁剪）。
- **FR-9.3** 松开左键提交为独立对象（可整体撤销；当前版本马赛克不支持选中/移动）。
- **FR-9.4** 预览、提交、导出三者的马赛克样式完全一致（同一像素化叠加图按半径缓存复用）。
- **验收**：涂抹区域像素块与预览一致，导出图片同效果，可撤销。

### FR-10 下载保存 【P0 已实现】

- **FR-10.1** 点击「下载」或按 `⌘S`，将**当前编辑后的完整图片**（按原图分辨率合成）保存到本地。
- **FR-10.2** 保存行为受设置控制，二选一：
  - 模式 A（默认）：直接保存到「默认保存位置」。
  - 模式 B：设置开启「保存时询问位置」后，每次弹出系统保存面板（`NSSavePanel`）供用户选择路径与文件名。
- **FR-10.3** 文件名规则：`zeroflow_yyyyMMdd_HHmmss.png`（如 `zeroflow_20260808_153012.png`）；同目录重名自动追加 `-1`、`-2`…避免覆盖。
- **FR-10.4** 保存格式：PNG（无损）。
- **FR-10.5** 保存成功后记录最近保存路径（供菜单「打开最近截图」使用），**不自动关闭编辑页**；保存失败弹系统警告框。
- **验收**：默认模式保存到配置目录；开启询问后每次弹面板；连续保存文件名不重复。

### FR-11 复制到剪贴板 【P0 已实现】

- **FR-11.1** 点击「复制」或按 `⌘C`，将编辑后的完整图片（PNG 数据）写入系统剪贴板（`NSPasteboard`，类型 `png`）。
- **FR-11.2** 复制结果可粘贴到任意支持图片的 App（微信、iMessage、Pages、预览等）。
- **FR-11.3** 复制后**不自动关闭编辑页**。
- **验收**：复制后直接打开「预览」粘贴可得到完整编辑后的图片。

### FR-12 设置页 【P0 已实现】

设置页是本软件独立的配置中心，所有配置项分组展示、修改后**立即生效并即时持久化**（无「保存」按钮，无需重启）。

- **FR-12.0.1** 入口：菜单栏图标 →「设置…」（`⌘,`）；设置窗口为单例（重复打开只聚焦已存在窗口，不重复创建）。
- **FR-12.0.2** 窗口规格：约 560×640 pt，`titled + closable`，居中显示于主屏。
- **FR-12.0.3** 所有设置修改后立即生效并即时持久化。
- **FR-12.0.4** 持久化：统一走 `SettingsStore`（`UserDefaults`）；存储 key 见 FR-12.9。
- **FR-12.0.5** 关闭窗口不丢失任何已修改项；重新打开时显示当前持久化值。
- **FR-12.0.6** 顶部分段控件切换三个标签页：
  ```
  设置
   ├── 通用
   │     └── 开机自动启动（开关）
   ├── 截图
   │     ├── 启用截图功能（总开关）
   │     ├── 截屏快捷键（按键录制框 + 恢复默认）
   │     ├── 屏幕录制权限（绿色=已授权 / 橙色警告 + 授权/重检按钮，开启截图后显示）
   │     └── 保存
   │           ├── 默认保存位置（路径 + 选择… + 打开）
   │           └── 保存时询问位置（开关）
   └── Dock
         ├── 单击 Dock 图标最小化窗口（开关）
         ├── 辅助功能权限引导（开启后显示）
         └── 「桌面与 Dock」前往设置 + 提示
  ```

#### FR-12.1 截图功能总开关
- **FR-12.1.1** 「启用截图功能」开关（key：`screenshotEnabled`，默认**关**）。
- **FR-12.1.2** 关闭时：全局热键不注册、菜单「立即截图」置灰、触发截图被忽略。
- **FR-12.1.3** 开启时若未授权屏幕录制，自动弹出系统授权（`CGRequestScreenCaptureAccess`）。

#### FR-12.2 截屏快捷键设置
- **FR-12.2.1** 展示当前快捷键，形式为一个可点击的「按键录制框」，显示形如 `⇧⌘S` 的组合。
- **FR-12.2.2** 点击按键录制框进入「录制」状态（框高亮 + 提示「按下新快捷键…」），等待用户按键；`Esc` 取消录制。
- **FR-12.2.3** 校验规则：必须包含至少一个修饰键（⌘/⌃/⌥/⇧）且主键为非修饰键，否则提示「请至少按下一个修饰键」并继续录制。
- **FR-12.2.4** 录制期间暂停全局热键（避免按下相同组合触发截图），录制结束恢复。
- **FR-12.2.5** 与当前设置相同 → 直接退出录制。
- **FR-12.2.6** 冲突处理：新快捷键注册失败（被系统或其他 App 占用）时热键不生效、旧热键仍保留，不崩溃；用户可再次录制修改。
- **FR-12.2.7** 「恢复默认」按钮：一键恢复 `⌘⇧S` 并重新注册。
- **FR-12.2.8** 保存 key：`globalShortcut.keyCode` + `globalShortcut.modifiers`。
- **FR-12.2.9** 修改后立即更新全局热键（`GlobalHotkeyManager` 监听 `zeroflow.shortcutDidChange` 通知自动重注册）。

#### FR-12.3 开机自启
- **FR-12.3.1** 「开机自动启动（登录时打开）」开关（key：`launchAtLogin`，默认**开**）。
- **FR-12.3.2** 通过 `SMAppService.mainApp`（macOS 13+）注册/注销 LaunchAgent。
- **FR-12.3.3** 启动时自动把持久化开关值与系统登录项实际注册状态对齐（修复「默认开但重启不自动启动」）。
- **FR-12.3.4** 注册失败自动回滚开关。
- **验收**：开关打开后，系统「登录项」中包含本 App；重启后自动运行。

#### FR-12.4 默认保存位置
- **FR-12.4.1** 一行显示「默认保存位置」+ 当前路径（截断显示，可复制），右侧按钮：`选择…`（`NSOpenPanel` 选目录）、`打开`（Finder 打开）。
- **FR-12.4.2** 默认值：`~/Downloads/zeroflow`；首次使用或读取缺失时自动写入并创建目录（`FileManager.createDirectory`）。
- **FR-12.4.3** 用户选择的路径写入设置（key：`saveDirectory`）；不存在则尝试创建，失败提示。
- **验收**：默认返回 `~/Downloads/zeroflow`；选择新目录后立即更新、且下载功能使用新目录。

#### FR-12.5 保存询问策略
- **FR-12.5.1** 「保存时询问位置」开关（key：`askSaveLocation`，默认**关**）。
- **FR-12.5.2** 关：每次保存直接存到默认目录；开：每次弹 `NSSavePanel`。
- **验收**：切换开关后，立即按「下载」验证行为变化正确。

#### FR-12.6 Dock 单击最小化开关
- **FR-12.6.1** 「单击 Dock 图标最小化窗口」开关（key：`dockClickMinimize`，默认**关**）。
- **FR-12.6.2** 开启后立即生效并持久化；关闭后完全恢复系统默认行为（移除事件监听）。
- **FR-12.6.3** 开启时若未授权辅助功能，自动弹出系统授权（`AXIsProcessTrustedWithOptions`）。
- **FR-12.6.4** 开启后显示辅助功能权限状态行（绿=已授权 / 橙=引导授权）与「桌面与 Dock」前往设置按钮（建议开启「最小化窗口到应用图标」）。

#### FR-12.7 系统权限指引
- **FR-12.7.1** 截屏权限引导内联在设置页「截图」标签页：已授权显示绿色「屏幕录制权限已开启」，未授权显示橙色警告 + 「打开系统设置授权 / 重新检测」按钮。
- **FR-12.7.2** 授权后回到 App 自动重查（真实探测，避免 preflight 缓存误判）。
- **FR-12.7.3** 未授权时触发截图：不弹窗，自动打开设置窗口并切换到「截图」标签页展示权限引导；禁止进入选区模式（避免产生全黑截图）。

#### FR-12.8 工具栏默认属性（预留分组，P1 未实现）
- 预留「默认画笔颜色」「默认画笔粗细」设置项，P1 优先级，当前版本未实现。

#### FR-12.9 设置数据持久化（UserDefaults Key 汇总）
| Key | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `globalShortcut.keyCode` | Int | 1 (s) | 全局截屏快捷键键码 |
| `globalShortcut.modifiers` | Int | cmd+shift | 全局截屏快捷键修饰键 |
| `launchAtLogin` | Bool | true | 开机自启 |
| `saveDirectory` | String | `~/Downloads/zeroflow` | 默认保存目录 |
| `askSaveLocation` | Bool | false | 保存时是否询问位置 |
| `screenshotEnabled` | Bool | false | 截图功能总开关 |
| `dockClickMinimize` | Bool | false | 单击 Dock 图标最小化窗口 |
| `cmdTabSwitcherEnabled` | Bool | false | 内置 ⌘⇥ 窗口切换器总开关 |
| `windowSwitcherShowWindowlessApps` | Bool | true | 是否显示无窗口但仍在运行的应用（占位卡排最后） |
| `cmdTabShortcut.keyCode` | Int | 48 (tab) | 切换器快捷键主键键码（固定两选一：48=Tab / 50=「`」键） |
| `cmdTabShortcut.modifiers` | Int | cmd | 切换器快捷键修饰键（⌘⇥=cmd / ⌥`=option） |
| `lastSavedPath` | String | nil | 最近保存路径（菜单「打开最近截图」用） |

### FR-13 菜单栏驻留 【P0 已实现】

- **FR-13.1** 常驻 `NSStatusItem` 菜单栏，图标为 `bolt.fill`。
- **FR-13.2** 菜单项（自上而下）：
  - `立即截图（⌘⇧S）` → 进入截屏模式（显示当前快捷键；截图总开关关闭时置灰）
  - `设置…（⌘,）`
  - `打开最近截图` → Finder 打开最近保存的图片（仅当存在最近保存记录时显示）
  - `退出 zeroflow（⌘Q）`
- **FR-13.3** 无 Dock 图标（`LSUIElement = YES`）。

### FR-14 单击 Dock 图标最小化窗口 【P0 已实现】

- **FR-14.1** 能力：单击 Dock 上「当前前台已激活 app」的图标，最小化该 app 的**聚焦窗口**（最近操作的窗口；取不到回退主窗口）。恢复窗口完全交给 macOS 原生行为（再点一次 Dock 图标即恢复），本模块只做「最小化」这一个方向。
- **FR-14.2** 判定：被点击 app == 当前前台 app（`NSWorkspace.frontmostApplication`），且该 app 在当前空间存在**可见、非全屏、未最小化**的窗口。
- **FR-14.3** 场景：
  - 单击非前台 app / 全屏中 app / Launchpad、废纸篓等无 app 身份项 / option、control 点击 → 放行给系统，不干预。
  - 该 app 窗口已全部最小化 → 放行给系统恢复。
- **FR-14.4** 实现：`CGEventTap`（`cghidEventTap` + 左键按下）识别 Dock 图标（AX dock item frame 缓存快路径 + `AXUIElementCopyElementAtPosition` 兜底命中），成功后吞掉事件（避免「再点即恢复」抵消最小化），最小化动作异步执行（事件回调内只识别 + 吞事件）。
- **FR-14.5** 多显示器 / 多 Space：只处理落在当前屏幕、当前空间的可见非全屏窗口；其他一律不动。
- **FR-14.6** 依赖辅助功能权限：未授权时开关仍可开但模块静默跳过，设置页展示引导；授权返回后自动生效，无需重启。
- **FR-14.7** 事件 tap 失效（权限被收回/系统暂停）时每 2s 周期自动重建，不崩溃。
- **验收**：开启开关后，任意前台 app 单击其 Dock 图标 → 聚焦窗口滑入 Dock；多窗口 app 只收走一个窗口；再点一次恢复；关闭开关后完全恢复系统默认。

### FR-15～FR-23 内置 ⌘⇥ 窗口缩略图切换器 【P1 已实现】

> 完整需求（FR-15 功能开关 / FR-16 ⌘⇥ 拦截 / FR-17 窗口列表 / FR-18 缩略图 / FR-19 激活 / FR-20 面板 / FR-21 设置页 / FR-22 权限 / FR-23 忽略名单预留）见第 12 章「内置 ⌘⇥ 窗口缩略图切换器（详细需求，原独立文档已合并至此）」。此处仅列要点。

- **FR-15** 功能开关：`cmdTabSwitcherEnabled`（默认关）；开关打开即安装 ⌘⇥ 事件 tap、立即生效，关闭即完全恢复系统默认 ⌘⇥ 行为、零残留；与截图 / Dock 模块互不干扰；未授权辅助功能时自动弹系统授权，授权返回自动生效；触发快捷键**固定两选一**（`cmdTabShortcut`，默认 `⌘⇥`，备选 `` ⌥` ``），修改后立即生效；不提供自由录制。
- **FR-16** ⌘⇥ 拦截：`CGEvent.tapCreate(.cghidEventTap, .headInsertEventTap, .defaultTap)` 拦截 keyDown/keyUp/flagsChanged 并吞掉；tap 被系统暂停时 `tapEnable` 恢复；创建失败每 2s 重试；按住配置修饰键每按一次配置主键前进一格（含连发）、+⇧ 后退、**会话中 ←/→/↑/↓ 方向键按网格移动选择**、Esc 取消、松开配置修饰键立即激活选中窗口；回调内只做决策+吞事件，UI/抓图/激活全部异步。
- **FR-17** 窗口列表：公开 API `CGWindowListCopyWindowInfo(.optionAll)` 枚举，过滤 layer==0、正常 app owner、bounds ≥ 2pt，**保留最小化/离屏窗口**；**幽灵窗口过滤**（CGS/SLS 私有 API：可见/全量成员列表 + Space 归属 + 最小化 tags 位 60，对齐 AltTab PhantomWindowDetector；模糊情形再查 AX subrole 需为标准窗口；CGS 不可用退回高度/比值启发式）；**窗口级 MRU 排序**（`WindowActivityTracker` 监听 AX 焦点通知，最近激活在前）；**无窗口但运行中的正常 app 显示占位卡（app 图标）并排最后**（`windowSwitcherShowWindowlessApps` 开关，默认开）；**不排除本 app**（设置窗口可见）；标题空时回退 app 名；默认列出所有 Space（`windowSwitcherAllSpaces` 可关，尽力而为）。
- **FR-18** 缩略图：SkyLight 私有 API `CGSHWCaptureWindowList`（运行时 dlsym，选项位 `1<<11|1<<8|1<<19`=0x80900），能抓最小化窗口；后台串行队列**逐窗口**调用（该 API 只认列表第一个，不批量）；按 CGWindowID 缓存（TTL 5s）+ 800ms 节流；未就绪/失败回退 app 图标；API 失效时静默降级不崩溃。
- **FR-19** 激活：最小化窗口先 AX 还原（`kAXMinimized=false`），再 `app.activate`，再 AX 置顶（`kAXMain` + `kAXRaiseAction`）；窗口匹配首选私有桥 `_AXUIElementGetWindow`，兜底 AX bounds 匹配；AX 写操作放后台串行队列；**本 app 窗口走主线程直接 `makeKeyAndOrderFront`（AX 在本进程内后台线程调用会触发 AppKit 崩溃）**；**无窗口 app 占位卡先 `launchApplication` 重开置前，失败退回 activate**。
- **FR-20** 面板：非激活 `NSPanel`（`.borderless,.nonactivatingPanel` + `.screenSaver` 层级，配置与选区遮罩一致）；SwiftUI 窗口卡（168×118，缩略图 + 标题 + app 名），**每行最多 6 个、宽度随数量自适应、不满行居中**，选中高亮描边+微放大、悬停高亮、点击即切换；**先抓缩略图就绪后再弹（250ms 兜底），无图标闪烁**；**悬停卡片左上角显示 4 个 12pt 红黄绿紫按钮（退出应用/关闭/最小化/全屏，不移动选择），操作后从面板移除该窗口**；居中于鼠标所在屏幕；会话结束销毁内容状态。
- **FR-21** 设置页「切换」标签页：总开关 + 固定两选一快捷键（`Picker(.radioGroup)`：`⌘⇥` / `` ⌥` ``）+ 权限引导（抽取公共 `AccessibilityPermissionRow`，与 Dock 共用）+「显示其他 Space 的窗口」开关 +「显示无窗口的应用」开关 + 屏幕录制权限实时状态行。
- **FR-22** 权限：辅助功能（吞事件 + AX 激活；未授权时模块静默、设置页橙色引导）；屏幕录制（真实缩略图与完整标题；无权限回退 app 图标 + app 名，切换仍可用）。
- **FR-23** 忽略名单：预留 `cmdTabSwitcherIgnoredApps` key，第一版不实现。
- **验收**：开启后按所选快捷键（⌘⇥ 或 ⌥`）弹缩略图面板且系统切换器不出现；⇥/⇧⇥/连发/方向键(←/→/↑/↓ 网格移动，边缘不累积)/Esc/松修饰键切换正确；切换两选一快捷键立即生效；同 app 多窗口按最近使用排序、默认选中上一个窗口；最小化、⌘H、其他 Space 窗口均可列可切；微信/Chrome 等 app 不出现空幽灵卡；切换到本 app 设置窗口不崩溃；**悬停卡片左上角 12pt 红黄绿紫按钮可退出/关闭/最小化/全屏（悬停不移动选择）且卡片即时移除**；**关掉某 app 全部窗口（不退）后 ⌘⇥ 末尾出现其 app 图标占位、点击激活、可关闭开关隐藏**；关闭开关后系统 ⌘⇥ 完全恢复；`ZEROFLOW_SWITCHER_DEBUG=1` 可打日志（含 phantomDetection / VERDICT）。

---

## 4. 数据模型与图像处理逻辑（实现级参考）

### 4.1 文档模型
- `EditDocument`：`image`（`NSImage` 原始截图）+ `elements`（`[CanvasElement]` 编辑对象列表）+ 撤销/重做双栈。

### 4.2 CanvasElement 统一类型（4 种）
| 类型 | 字段 | 可否选中 |
|---|---|---|
| `.stroke` | 点集 `[CGPoint]`、颜色 `Color`、线宽 `CGFloat` | 否（可整体撤销） |
| `.shape` | 框 `CGRect`、样式 `rect/ellipse`、颜色、线宽 | 是（移动/缩放/删除） |
| `.bubble` | 锚点、文本框 `CGRect`、文字 | 是（移动/缩放/锚点/删除/双击编辑） |
| `.mosaic` | 点集 `[CGPoint]`、半径 `CGFloat` | 否（可整体撤销） |

### 4.3 渲染
- 在原始截图上按顺序重绘所有 `CanvasElement`，合成最终 `NSImage`；
- 下载 / 复制均使用该合成图（原图物理像素分辨率）；
- 马赛克渲染：整图最近邻像素化叠加图按半径缓存（`MosaicOverlayCache`），按圆形笔刷裁剪，预览/提交/导出共用同一张图。

### 4.4 撤销栈
- 双栈实现（undo/redo），容量上限 50；任何修改前先 `snapshotBeforeChange()`。

### 4.5 多屏坐标
- 每个屏幕一个遮罩窗口，选区矩形在该屏幕坐标系内（左下原点）；
- ScreenCaptureKit `sourceRect` 为左上原点 → `ScreenCapture.topLeftRect` 负责换算；
- Retina 按 `backingScaleFactor` 换算物理像素。

---

## 5. 界面与交互规范

| 规范 | 说明 |
|---|---|
| 图标风格 | 线性图标（SF Symbols），选中/未选中状态区分（高亮底色 + 描边） |
| 工具栏样式 | 系统控制背景色（`controlBackgroundColor`），顶部细分隔线，高约 28pt |
| 悬停反馈 | tooltip 显示按钮名称与快捷键 |
| 交互反馈 | 全部反馈 100ms 内响应，无加载动画 |
| 窗口 | 编辑页带标题栏、可缩放，初始尺寸按截图自适应 |
| 主题 | 跟随系统深色/浅色切换 |
| 语言 | 界面文本简体中文 |
| 无障碍 | 按钮提供 `.help` tooltip |

---

## 6. 性能与质量目标

| 指标 | 目标 |
|---|---|
| 启动时间 | 冷启动到菜单栏就绪 ≤ 0.5s；快捷键到选区 ≤ 0.3s |
| 内存占用 | 驻留 ≤ 80MB，空闲无后台轮询定时器 |
| 截图延迟 | 松开鼠标到编辑页可见 ≤ 300ms |
| 选区渲染 | 拖拽 resize 实时渲染无卡顿（60fps 目标） |
| 马赛克 | 像素化叠加图按半径缓存，涂抹实时不卡顿 |
| 稳定性 | 选区/绘制过程中不允许未捕获异常；异常记录日志（`/tmp/zeroflow.log`） |
| 磁盘 | 除用户主动保存的文件外，无残留缓存 |

---

## 7. 边缘场景与异常处理

1. **未授权屏幕录制** → 打开设置页「截图」标签页引导，禁止截取，不崩溃。
2. **多显示器且缩放比例不同** → 每屏一个遮罩窗口，选区按鼠标所在屏，尺寸换算按物理像素。
3. **原地单击（<1px）** → 截全屏；仅单边 ≥1px → 取消。
4. **快捷键被其他 App/输入法占用注册失败** → 热键不生效、旧热键保留，设置页可再修改。
5. **截图总开关关闭** → 热键不注册、菜单置灰、触发忽略。
6. **保存目录不可写** → 弹系统警告框提示。
7. **编辑页打开时再次触发截图** → 忽略本次触发（防重入），避免中断当前编辑。
8. **Dock 单击最小化未授权辅助功能** → 模块静默跳过，设置页引导。
9. **事件 tap 失效**（Dock 模块）→ 周期自动重建，不崩溃。

---

## 8. 兼容性

| 项 | 要求 |
|---|---|
| 系统版本 | macOS 14.0（Sonoma）及以上 |
| 显示 | 支持 Retina、缩放分辨率、多显示器 |
| 芯片 | Apple Silicon 与 Intel 均支持（通用二进制） |
| 语言 | 首版简体中文 |

---

## 9. 里程碑规划

| 阶段 | 内容 | 状态 |
|---|---|---|
| **M1（MVP 基础）** | 菜单栏驻留、`⌘⇧S` 选区、编辑页、撤销/重做、画线、文字气泡、下载/复制、权限引导 | ✅ 已实现 |
| **M1.5（扩展工具）** | 矩形/圆框标注、马赛克涂抹 | ✅ 已实现 |
| **M2（全链路）** | 全局热键真接入、设置项全链路生效、开机自启修复 | ✅ 已实现 |
| **Dock 模块** | 单击 Dock 图标最小化窗口 + 辅助功能引导 | ✅ 已实现 |
| **切换器模块** | 内置 ⌘⇥ 窗口缩略图切换器（事件 tap + 窗口列表/缩略图/激活 + 设置页「切换」标签页） | ✅ 已实现 |
| **M3（体验打磨）** | Toast、视觉规范、多屏深测、Developer ID 签名与 Notarization | ⏳ 部分完成，待公证 |
| **M4（可选）** | 长截图、贴图置顶、图形（箭头）等扩展 | 未开始 |

---

## 10. 验收标准总纲

1. 默认在任何 App 中按 `⌘⇧S`，立即进入十字光标截图；选区、尺寸提示、Retina 尺寸均正确。
2. 可绘制任意颜色/粗细的线、矩形、圆框、马赛克，并可在画布上撤销/重做而不丢失内容。
3. 可添加文字气泡并移动、缩放、重新编辑、删除、撤销，无需重启。
4. 下载覆盖两种模式：固定默认路径 / 每次询问路径；复制后粘贴到其他 App 有效。
5. 开启开机自启后，登录后图标驻留菜单栏，快捷键可正常截图。
6. 开启「单击 Dock 图标最小化窗口」后，单击前台 app 的 Dock 图标只最小化其聚焦窗口。
7. 开启「窗口切换」后，⌘⇥ 弹出缩略图面板（系统切换器不出现），可切到最小化/同 app 多窗口/其他 Space 窗口；关闭后系统 ⌘⇥ 完全恢复。
8. 未授权屏幕录制/辅助功能时有正确引导且不崩溃。

---

## 11. 发布与分发

- 应用标识：`com.zeroflow.app`。
- 首次 Release 需通过 Developer ID 签名 + Notarization，提供 DMG 与 `brew` 安装方式。
- 需求追踪：FR-x.y 编号与验收条目一一对应，便于回归测试。

---

## 12. 内置 ⌘⇥ 窗口缩略图切换器（详细需求）

> 本章为原独立文档 `窗口切换器需求文档.md` 合并而来（实现已完成）。FR-15~23 编号延续主文档；第 3 章「FR-15～FR-23」一节为要点速览，本章为完整条款。

### 12.1 功能概述

#### 12.1.1 产品定位

zeroflow 增加一个「窗口切换器」模块：接管系统 **⌘⇥（Cmd+Tab）** 应用切换快捷键，改为显示 **窗口缩略图面板** 供用户选择切换，行为与 AltTab 一致。系统 Cmd+Tab 只能切 app、看不到具体窗口，本功能把它替换成「每个窗口一张缩略图」的切换器，并支持切换到系统 Cmd+Tab 切不到的窗口。

#### 12.1.2 能切到哪些系统 Cmd+Tab 切不到的窗口

| 场景 | 系统 Cmd+Tab | 本功能 |
|---|---|---|
| 同 app 有多个窗口（如 3 个 Finder 窗口） | 只能切 app，无法选具体窗口 | 每个窗口一张卡，可精确选 |
| 已最小化到 Dock 的窗口 | 切 app 后窗口还在 Dock | 列表给出缩略图卡，选中后自动还原 |
| 隐藏（⌘H）app 的窗口 | 不显示 | 正常列出 |
| 其他 Space 上打开的窗口 | 切不到 | 列出；选中后自动切到该 Space |
| 无窗口的 app | 不显示 | 默认不列（与 AltTab `showWindowlessApps = .hide` 一致，见下方判据说明） |

> **判据与 AltTab 一致**：以上覆盖范围与 AltTab 默认行为（AltTab: `src/switcher/state/WindowFilterResolver.swift` 的 `shouldShow`）**完全对齐，不额外扩大覆盖**——AltTab 默认不列出的窗口，本功能同样不列、也不尝试切：
> - 无窗口且非活动的后台 app（AltTab 默认 `showWindowlessApps = .hide`）→ 不列；本功能对应开关 `windowSwitcherShowWindowlessApps`（默认开）；
> - 被系统判定为 phantom 的窗口 → 不列；
> - 被用户设置忽略名单排除的 app → 不列（第一版可不做忽略名单，见 FR-23 预留）。

#### 12.1.3 目标交互（手感与系统 ⌘⇥ 一致）

> 触发快捷键**可在设置中修改，默认 ⌘⇥**（见 FR-15/FR-21）。下文以默认 ⌘⇥ 描述交互；改为其他组合键（如 ⌥⇥）时逻辑不变，只是「按住 ⌘」换成「按住配置的修饰键」。

```
按住 ⌘ → 按 ⇥ → 弹出缩略图面板（系统切换器被完全吞掉，不出现）
     → 继续按 ⇥  前进一格 / ⇧⇥ 后退一格 / Esc 取消
     → 松开 ⌘ → 激活选中窗口，面板收起
```

- 按住 ⌘ 后**每按一次 ⇥** 前进一格（含按住自动连发），到末尾后环绕回开头。
- **⇧⌘⇥** 后退一格（到开头后环绕回末尾）。
- 松开 ⌘ 立即切换（不再需要再按一下）。
- **Esc** 取消切换：只关闭面板，不改变前台 app。
- 面板打开期间鼠标可悬停/点击某个窗口卡直接切换（点击即切换）。
- 面板内方向键：←/→ 与 ⇥/⇧⇥ 左右循环；↑/↓ 在相邻行内移动（无相邻行则原地不动）。

#### 12.1.4 术语

| 术语 | 含义 |
|---|---|
| 切换会话 | 从 ⌘⇥ 触发面板弹出，到松 ⌘/Esc 面板销毁的一段生命周期 |
| 窗口卡（Tile） | 面板中每个窗口的展示单元：缩略图/图标 + 标题 + app 名 |
| 最小化窗口 | 已 miniaturize 到 Dock 的窗口（`kAXMinimized=true`） |
| 系统切换器 | macOS 自带的 app 图标切换器（Dock 负责处理 ⌘⇥） |

#### 12.1.5 性能与质量目标

- 热键到面板可见 ≤ 200ms（参照主需求「快捷键到选区 ≤ 300ms」）。
- 缩略图抓取不卡界面：全部在后台，面板先显示占位图标再逐张刷新。
- 同一会话内相同窗口不重复抓图；再次召唤时优先复用缓存（TTL 见 12.5）。
- 驻留内存增幅受控（缩略图按窗口卡分辨率、有上限并回收），不破坏「驻留 ≤ 80MB」目标。
- 模块关闭时**完全**恢复系统默认 ⌘⇥ 行为，零残留。

### 12.2 整体工作流程

```
菜单栏「设置 → 切换」标签页启用开关
     │  （cmdTabSwitcherEnabled = true）
     ├─ 启动「切换」模块：安装 ⌘⇥ 事件 tap（需辅助功能权限）
     │
     ├─ 用户按 ⌘⇥
     │     ↓ 事件 tap 吞掉 Tab/Cmd keyDown/up（系统切换器不出现）
     │     ↓ 枚举窗口列表（CGWindowList 公开 API）→ 按 app 分组
     │     ↓ 弹出缩略图面板（NSPanel .screenSaver 非激活层）
     │     ↓ 后台批量抓取各窗口缩略图（SkyLight 私有 API，最小化窗口也能抓）
     │     ↓ ⇥/⇧⇥/Esc/鼠标悬停/点击 操作选择
     │     ↓ 松开 ⌘ → AX 还原+前置+激活选中窗口 → 面板收起
     │
     └─ 设置页「切换」标签页：开关 + 辅助功能权限引导(绿=已授权/橙=引导)
```

### FR-15～FR-23 内置 ⌘⇥ 窗口缩略图切换器（完整条款）

> 编号延续主需求文档（主需求已到 FR-14）。

#### FR-15 功能开关与入口

- **FR-15.1** 「窗口切换」（模块名）总开关，key：`cmdTabSwitcherEnabled`，默认**关**。
- **FR-15.2** 打开开关即安装 ⌘⇥ 事件 tap，立即生效；关闭即移除 tap，系统 ⌘⇥ 完全恢复，无残留。
- **FR-15.3** 总开关关闭状态下：不安装 tap、不弹面板、不抓窗口、无任何行为。
- **FR-15.4** 开关与截图功能无关，可独立开启/关闭；与 Dock 单击最小化模块互不干扰（两者各自独立 CGEventTap，可共存）。
- **FR-15.5** 打开开关时若未授权辅助功能权限，自动弹出系统授权（`AXIsProcessTrustedWithOptions`，与 Dock 模块一致）；授权返回后模块自动生效，无需重启。
- **FR-15.6** 触发快捷键**固定两选一**（无自由录制），默认 `⌘⇥`（Command + Tab）。key 见第 12.4 节：`cmdTabShortcut.keyCode` + `cmdTabShortcut.modifiers`；两个固定搭配：
  - `⌘⇥`：keyCode 48（kVK_Tab）+ `.command`（默认）；
  - `` ⌥` ``：keyCode 50（kVK_ANSI_Grave，Tab 上方 / 数字 1 左侧那枚 ` 键）+ `.option`。
  - 加载时若持久化的是「两选一」之外的旧值（此前自由录制遗留），一律钳回默认 `⌘⇥`。修改后立即生效（事件 tap 重新读取配置）。
- **FR-15.7** 不提供自由录制快捷键功能：切换页用固定 `Picker(.radioGroup)` 二选一，因而**不再需要**录制期间挂起/恢复本模块 tap（`suspend()`/`resume()` 已删除，与截图页 `GlobalHotkeyManager.suspend/resume` 不同，后者仍保留供截图快捷键录制使用）。

#### FR-16 ⌘⇥ 拦截

- **FR-16.1** 用 `CGEvent.tapCreate`，参数固定为：
  - `tap: .cghidEventTap`（HID 级最早介入点，能赶在系统/Dock 之前处理 ⌘⇥）
  - `place: .headInsertEventTap`
  - `options: .defaultTap`（**必须** defaultTap 才能吞事件，返回 nil 即吞掉）
  - `eventsOfInterest`: `(1 << keyDown) | (1 << keyUp) | (1 << flagsChanged)`
- **FR-16.2** `.defaultTap` 需要**辅助功能权限**；`tapCreate` 返回 nil（未授权）时模块静默不装 tap，设置页引导，并每 2s 周期重试（参照 `DockClickMinimizer` 的 `installRetryTimer` 模式）。
- **FR-16.3** tap 被系统暂停（`.tapDisabledByTimeout` / `.tapDisabledByUserInput`）时重新 `CGEvent.tapEnable(tap:enable:true)`（参照 AltTab: `src/events/KeyboardEvents.swift` 的 `reEnableTapIfNeeded`）。
- **FR-16.4** 事件处理规则（回调内只做**决策 + 吞事件**，UI/抓图全部异步）。下表「配置修饰键 / 配置主键」指设置中的 ⌘⇥ 快捷键（默认 ⌘ + Tab，可改）：
  | 事件 | 状态 | 处理 |
  |---|---|---|
  | flagsChanged，配置修饰键由按下→抬起（默认即 ⌘ 抬起） | 会话活跃 | 触发「激活选中窗口」→ 吞掉该事件（return nil） |
  | flagsChanged，配置修饰键抬起 | 会话不活跃 | 放行 |
  | keyDown，配置修饰键按住 + 配置主键（默认 ⌘+Tab，kVK_Tab=48） | 任意 | 吞掉；会话不活跃→开始会话+弹面板；活跃→前进一格（按住自动重复也前进） |
  | keyDown，配置修饰键 + Shift + 配置主键（默认 ⇧⌘⇥） | 任意 | 吞掉；后退一格 |
  | keyDown，Esc(kVK_Escape=53) | 会话活跃 | 吞掉；取消会话，收起面板 |
  | keyDown，←(123)/→(124)/↑(126)/↓(125) | 会话活跃 | 吞掉；←/→ 环绕 ±1 格；**↑/↓ 移动到相邻行里「渲染位置」（含末行居中偏移）最接近当前窗口的窗口，无相邻行则原地不动**；`selectionIndex` 都环绕/钳制在边界，tiles 未就绪时按键先累计不丢 |
  | keyDown 其他 | 会话活跃 | 放行（不做特殊处理） |
  | keyUp，配置主键 | 会话活跃 | 吞掉（避免系统收到残片） |
  | 其余 | — | 放行 |
- **FR-16.5** 自动重复：用 `keyboardEventAutorepeat` 字段识别重复的配置主键 keyDown，反复前进。
- **FR-16.6** 已知边界（记录，不强制解决）：密码框等 **Secure Input** 场景下系统会过滤 keyDown（flagsChanged 仍可达），此时可能在密码框内回退为系统切换器行为。
- **FR-16.7** 快捷键变化处理：`cmdTabShortcut` 变化 → 通知 `CommandTabSwitcher` 重新读取配置；会话进行中不打断（下一次触发生效）。

#### FR-17 窗口列表

- **FR-17.1** 用公开 API `CGWindowListCopyWindowInfo(.optionAll, kCGNullWindowID)` 枚举（`.optionAll` 含最小化/离屏窗口）。
- **FR-17.2** 提取字段：`kCGWindowNumber`、`kCGWindowOwnerPID`、`kCGWindowOwnerName`、`kCGWindowName`（标题）、`kCGWindowBounds`（JSON 解码为 CGRect）、`kCGWindowLayer`、`kCGWindowIsOnscreen`、`kCGWindowAlpha`、`kCGWindowWorkspace`。
- **FR-17.3** 过滤规则（全部满足才入列）：
  - `kCGWindowLayer == 0`（正常窗口层；Dock/菜单栏/状态栏 层值非 0 自动排除）；
  - owner 是正在运行的正常 app（`NSRunningApplication`），排除：`com.apple.dock`、`com.apple.WindowServer`、`com.apple.controlcenter`、`com.apple.systemuiserver` 等系统性进程；**不排除本 app**（`com.zeroflow.app`），否则设置窗口不会出现在切换器里（Zeroflow 的遮罩/编辑/切换面板都是高 layer，天然被过滤，只有设置窗口是 layer 0）；
  - 窗口 bounds 宽高 ≥ 2pt；
  - **保留** `kCGWindowIsOnscreen == false` 的窗口（最小化/离屏，正是系统切不到的窗口）。
  - **幽灵窗口过滤**（对齐 AltTab `PhantomWindowDetector` + `WindowDiscriminator`）：
    - 现象：alpha=0 的 Outlook 提醒、`orderOut:`/`show:false` 的 Electron 窗口、微信/Teams 隐藏窗口，以及 WindowServer 为真实窗口伴生的影子记录（如 Chrome 1272×498、微信 500×500）——存在但抓不到图、不是可切换目标。
    - **主判据（CGS/SLS 私有 API，符号缺失自动退回下面的启发式）**：
      - 可见列表 = `CGSCopyWindowsWithOptionsAndTags(..., options=1<<1)`（不含 `.invisible1/.invisible2` 标签）；全量列表 = options `1<<1|1<<0|1<<2`；
      - 每窗口 Space = `CGSCopySpacesForWindows(conn, .all)` 逐 Space 反查；当前可见 Space = `CGSCopyManagedDisplaySpaces` 的 Current Space；
      - 最小化 = SLS `tags & (1<<60)`；隐藏 app = `NSRunningApplication.isHidden`；
      - 判定顺序（对齐 AltTab `cgsVerdict`）：最小化/隐藏 app → 保留；不在全量列表 → 幽灵（强）；在可见列表 → 保留；Space 非当前可见 → 保留（其他 Space 真窗口）；否则 → 幽灵（弱）。
      - **AX subrole 兜底**：对「不可见的其他 Space 窗口」（上面「保留」分支的模糊情形）再查 AX subrole，非 `AXStandardWindow`/`AXDialog` 或取不到 AX 实体 → 幽灵（对齐 AltTab `isActualWindow`）。这能确定性剔除 Chrome 伴生记录（纯 CGS 成员判定会随当前 Space 变化而漏判）。
    - **兜底启发式**（CGS 不可用时的退化路径）：`height < 48` **或** `width/height > 6`（Chromium 伴生标题条 ≈12~15:1）**或**（标题为空 且 不在屏 且 `max(宽,高) < 400`）→ 排除。
    - 保证：真实窗口（含最小化/其他 Space/⌘H 的）有标题或尺寸足够而保留；即使标题为空，只要足够大也保留。
- **FR-17.4** 排序：**窗口级 MRU**（对齐 AltTab `lastActivityTime`）——用 `WindowActivityTracker` 监听各 app `kAXFocusedWindowChangedNotification`（`_AXUIElementGetWindow` 取 CGWindowID 打时间戳），另在每次切换激活后**同步补记**目标窗口（保证快速连按 ⌘⇥ 时顺序正确），全部窗口按最近激活时间降序；无记录按 app 级 MRU（前台 app 居前）兜底。这样 index 0 = 当前窗口、index 1 = 上一个窗口，默认选中（= 1）天然正确，同 app 多窗口也按最近使用排序。
- **FR-17.5** 标题：优先 `kCGWindowName`，为空回退 AX `kAXTitle`（如某些 app 不提供 CG 标题时，AltTab 同款 `bestEffortTitle` 思路）。
- **FR-17.6** Space：**固定列出所有 Space 的窗口**（永远不做空间过滤；旧版「显示其他 Space 的窗口」开关已删除，总是显示其他虚拟桌面的窗口，选中后自动切 Space）。
- **FR-17.7** 无窗口 app：开关 `windowSwitcherShowWindowlessApps`（默认**开**）开启时，**窗口全部关闭但仍运行的正常 app**（`activationPolicy == .regular` 有 Dock 图标、非排除名单、非本 app、当前列表无其窗口）显示为**占位卡（app 图标，无缩略图）**，**统一排在所有真实窗口之后**（内部按 app MRU 排序）。点击占位卡 = 激活该 app（无 AX 窗口走 `app.activate` 兜底）；悬停只显示「退出应用」按钮（关闭/最小化/全屏对无窗口 app 无意义）。
- **FR-17.8** 性能与缓存：每个会话内只枚举一次；监听 `NSWorkspace.didLaunchApplicationNotification / didTerminateApplicationNotification / NSApplication.didChangeScreenParametersNotification` 置缓存失效（参照 `DockClickMinimizer.observeDockLayoutChanges`）。

#### FR-18 窗口缩略图

- **FR-18.1** 缩略图来源：SkyLight **私有 API** `CGSHWCaptureWindowList`（与 AltTab 在 macOS 14/15 上生产做法一致：AltTab: `src/events/WindowCaptureEvents.swift` 的 `WindowCaptureScreenshotsPrivateApi.oneTimeCapture`）。它一次批处理 IPC 返回多个窗口的 `CGImage`，**且能截取最小化/离屏窗口**（公开 API ScreenCaptureKit 截不了最小化窗口）。
- **FR-18.2** 私有 API 调用方式（推荐运行时 `dlsym`，避免改工程 linker 配置）：
  ```swift
  // 函数签名（@_convention(c)）
  func CGSMainConnectionID() -> Int                // 取 WindowServer 连接
  func CGSHWCaptureWindowList(_ cid: Int,
                              _ wids: UnsafeMutablePointer<CGWindowID>,
                              _ count: Int32,
                              _ options: Int32) -> Unmanaged<CFArray>?  // 返回 [CGImage]
  // options 位值（AltTab 当前使用的 CGSWindowCaptureOptions，现代 macOS）：
  //   .ignoreGlobalClipShape = 1 << 11 (0x800)  跳过全局裁剪（含被遮挡窗口）
  //   .bestResolution        = 1 << 8  (0x100)  最优分辨率
  //   .fullSize             = 1 << 19 (0x80000)  全尺寸
  //   组合 = 0x80900。⚠️ 旧头文件的 0x04|0x10|0x20 已失效，传 0x34 会返回空。
  ```
  `dlsym` 加载 `/System/Library/PrivateFrameworks/SkyLight.framework/SkyLight`，`unsafeBitCast` 为上述类型后调用；`takeRetainedValue()` 取 `CFArray`，`as? [CGImage]` 转数组。抓到的图按窗口卡逻辑尺寸等比缩小后再展示。
- **FR-18.3** 抓图执行：全程在**后台串行队列**，**逐窗口调用** `CGSHWCaptureWindowList`（一次只传 1 个 `CGWindowID`）。该私有 API 实际只认窗口列表里的第一个（AltTab 源码注释），批量传会丢其余窗口，故不用 ≤16 分批。
- **FR-18.4** 缓存：按 `CGWindowID` 缓存抓到的缩略图；同一会话内不再抓已缓存的窗口；会话启动时缓存已存在且 TTL 未过期（建议 TTL ≈ 5s）直接复用。会话销毁时可清空对象引用（内存只随会话存活）。
- **FR-18.5** 节流：对单窗口不重复抓取（≥800ms 内不重复抓同窗口，参照 AltTab `screenshotThrottler`）。会话打开瞬间避免全量高并发抓图导致系统截图阻塞。
- **FR-18.6** 占位与降级：抓图完成前面板显示 app 图标（`NSRunningApplication.icon`）占位；抓取失败/被系统拒绝的窗口保留图标卡，不弹错不崩溃。
- **FR-18.7** 权限依赖：
  - 有屏幕录制权限：缩略图真实、窗口标题齐全；
  - 无屏幕录制权限：缩略图回退 app 图标、标题用 app 名（AX 标题需辅助功能权限），**切换功能仍可用**（AX 激活不依赖屏幕录制）。
- **FR-18.8** 若未来 macOS 移除/改名该私有 API：全部抓图失败 → 自动降级为图标卡，不影响列表与切换；记录日志（`ZSLog`）。

#### FR-19 切换 / 激活窗口

- **FR-19.1** 最小化窗口先还原：`AXUIElementSetAttributeValue(axWindow, kAXMinimizedAttribute, kCFBooleanFalse)`（AltTab: `src/switcher/state/Window.swift` focus() 第 397-399 行同款）。还原动画结束前（约 0.4–0.7s）不对该窗口重新抓图（参照 AltTab `WindowThumbnails.deferCaptureUntilRestoreEnds` 思路，避免抓到迷你帧）。
- **FR-19.2** 激活 app：`app.activate(options: [.activateAllWindows, .activateIgnoringOtherApps])`。注意 macOS 14 起该 API 降级为「建议性请求」，不能单独依赖，必须叠加下面 AX 步骤。
- **FR-19.3** AX 精确定位目标窗口：
  1. `AXUIElementCreateApplication(pid)` → 取 `kAXWindowsAttribute` 枚举窗口；
  2. 匹配 CGWindowID（二选一）：
     - 私有单行桥 `_AXUIElementGetWindow(axWindow, &wid)`（`@_silgen_name("_AXUIElementGetWindow")` 声明，链接 `ApplicationServices.framework`；AltTab: `src/macos/api-wrappers/HIServices.framework.swift`）——**推荐**；
     - 纯公开兜底：以 AX 窗口 `kAXPosition`+`kAXSize` 与 CGWindow bounds 匹配（AltTab: `src/switcher/state/BruteForceWindowMatch.swift` 曾用思路）。
  3. 置顶：`AXUIElementSetAttributeValue(axWindow, kAXMainAttribute, kCFBooleanTrue)` + `AXUIElementPerformAction(axWindow, kAXRaiseAction)`。
- **FR-19.4** 跨 Space：目标窗口在其他 Space 时，上述 activate + raise 通常会让 macOS 自动切换 Space（AltTab 本节有额外加固，v1 不做：`_SLPSSetFrontProcessWithOptions`/`SLSSpaceSetFrontPSN` 等私有进程管理接口）。
- **FR-19.5** 无窗口 app 占位卡：`app.activate` 对无窗口 app 通常无效，**先 `NSWorkspace.shared.launchApplication(at: bundleURL)` 重新启动置前**（多数 app 会尝试重开窗口），失败退回 `app.activate`（对齐 AltTab `Window.focus` 的 windowless 分支）。
- **FR-19.6** 线程：AX 写操作放到专门的后台串行队列执行，绝不在事件 tap 回调里跑 AX（回调只做决策+吞事件，与 Dock 模块同一原则）。
- **FR-19.7** 本 app 窗口特例：目标是 Zeroflow 自己的窗口时**不走 AX**——AX 在本进程内会被 AppKit 转成 `[NSWindow makeKeyAndOrderFront:]`，后台线程调用会触发 AppKit 崩溃（SIGTRAP，实测崩溃日志）。改为主线程：`NSApp.activate(ignoringOtherApps:)` + 按 `windowNumber == Int(wid)` 找 NSWindow 直接 `makeKeyAndOrderFront(nil)`（最小化先 `deminiaturize`）。
- **FR-19.8** `_AXUIElementGetWindow` 不可用时（策略顾虑）：走 FR-19.3 的公开兜底匹配，功能不依赖任何私有 AX 桥也可工作。

#### FR-20 缩略图面板

- **FR-20.1** 面板窗口 `NSPanel`，配置与现有截图遮罩窗口 `CaptureOverlayWindow`（`Zeroflow/Views/CaptureOverlayView.swift`）一致：
  - `styleMask: [.borderless, .nonactivatingPanel]`；`level = .screenSaver`；
  - `isOpaque = false`、`backgroundColor = .clear`、`hasShadow = true`；
  - `collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary, .stationary]`；
  - `isReleasedWhenClosed = false`；
  - `canBecomeKey = false`、`canBecomeMain = false`（面板永不夺走原 app 的 key/main，否则会关闭用户现有弹出框——与遮罩窗口同样的理由）。
- **FR-20.2** 键事件：面板不接收键盘（canBecomeKey=false），前进/后退/取消/激活全部由 FR-16 的事件 tap 处理；鼠标悬停/点击用 SwiftUI 手势处理。
- **FR-20.3** 内容（SwiftUI `NSHostingView` 承载）：
  - 手动分行网格（每行最多 **6 个**卡片，卡片 ≈ 168×118pt 逻辑尺寸）展示窗口卡；窗口**宽度随窗口数量自适应**（不足 6 个时按实际列数收窄，超过则换行）；**不满的一行内容居中**（每行 `HStack(spacing:12) + frame(maxWidth:.infinity)`），网格整体纵向滚动：
    - 选中态：高亮描边（如蓝色 2pt 圆角边框）+ 轻微放大；
    - 卡片主体：缩略图（未就绪显示 app 图标占位）；
    - 卡片底部：窗口标题（单行截断）＋ 小号 app 名。
  - 悬停：`onHover` 改变高亮底色 + 显示该卡片左上角的操作按钮（**不移动选择**，选择只由键盘 Tab/方向键控制）；点击：立即切换该窗口。
- **FR-20.4** 定位：面板居中于**鼠标当前所在屏幕**（`NSScreen.screens.first{ $0.frame.contains(NSEvent.mouseLocation) }`），多屏适配。
- **FR-20.5** 打开/关闭：**先抓缩略图、就绪后再 `orderFront`**（避免「先 app 图标后缩略图」的闪烁；250ms 兜底计时器防止抓图过慢时面板迟迟不出现，未就绪瓦片仍显示 app 图标占位）。`orderOut` 收起；会话结束（激活或取消）时销毁面板内容状态。面板弹起后再补一次 `isVisible` 复检，全屏 Space 下偶发不显示时重新 `orderFront`。
- **FR-20.6** 卡片操作按钮（对齐 AltTab WindowOps）：
  - 悬停卡片时**左上角**（贴卡顶边，`alignment: .topLeading`）显示 4 个圆形按钮，**系统红绿灯样式（直径 12pt，与 macOS 一致）**：`power`(退出应用，紫) / `xmark`(关闭，红) / `minus`(最小化，黄，符号用黑) / `arrow.up.left.and.arrow.down.right`(全屏切换，绿)；按钮自带 tap 手势，优先于整卡切换，点击不触发选中激活，会话保持打开；**鼠标悬停到某个按钮时该按钮微放大（仅当前按钮）**。
  - 执行（`WindowOps`）：
    - **本 app 窗口 → 主线程 NSWindow**：退出=`NSApp.terminate`、关闭=`performClose`、最小化=`miniaturize`、全屏=`toggleFullScreen`（绕开 AX 后台线程操作本进程窗口的崩溃，同 FR-19.7）；
    - **其他 app → 后台 AX**：退出=`app.terminate()`、关闭=**按 AX 关闭按钮（`AXCloseButton` + `AXPress`，即系统红绿灯路径；多数 app 不实现窗口的 `AXClose` action）**、最小化=`kAXMinimized=true`、全屏=`AXFullScreen` 取反写入。
  - 操作完成后从面板移除受影响窗口（退出应用移除该 pid 全部窗口），clamp 选中；列表空则收起面板。
  - AX 窗口匹配复用 `AXWindow.element`（`_AXUIElementGetWindow` + bounds 兜底），与激活共用。

#### FR-21 设置页「切换」标签页

- **FR-21.1** `SettingsTab` 枚举新增 `case switcher = "切换"`（放在「截图」与「Dock」之间）。标签页按项目现有 `Form` + `.formStyle(.grouped)` 风格实现。
- **FR-21.2** 顶部 Section「启用」：
  - `Toggle("用 ⌘⇥ 显示窗口缩略图", isOn: $store.cmdTabSwitcherEnabled)`；
  - footer：`"开启后，按住 ⌘ 再按 ⇥ 会弹出窗口缩略图切换器，可切换到最小化、同 app 多窗口、其他 Space 的窗口。关闭后完全恢复系统默认。"`
- **FR-21.3** 开关打开时（`cmdTabSwitcherEnabled` 变化触发，参照截图总开关的 `onChange` 写法）：
  - 若未授权辅助功能权限 → `AccessibilityPermission.requestAuthorization()`。
- **FR-21.4** 快捷键 Section（开关开启后显示）：
  - 固定两选一单选框，不提供自由录制／自定义（见 FR-15.6/15.7）：
    ```
    Section {
        Picker("切换快捷键", selection: $store.cmdTabShortcut) {
            Text("⌘⇥").tag(ShortcutKey.cmdTabDefault)
            Text("⌥`").tag(ShortcutKey.optionGraveDefault)
        }
        .pickerStyle(.radioGroup)
    }
    ```
  - 直接把 `cmdTabShortcut` 设为两个固定 `ShortcutKey` 之一（`ShortcutKey: Hashable`），didSet 通知走 `CommandTabSwitcher.shortcutDidChangeNotification`，下一次触发即生效；
  - footer 说明：`"选择其一弹出切换器：⌘⇥（Command + Tab）或 ⌥`（Option + Tab 上方的「`」键）。⇧ + 修饰键 + 主键 后退。"`
- **FR-21.5** 权限引导 Section（开关开启后显示；**独立按钮**，不直接复用/联动 Dock 模块的行，即使两者是同一个辅助功能权限）：
  - 样式与 `DockPermissionRow` 完全一致，制作为**可复用组件** `AccessibilityPermissionRow`（把现有 `DockPermissionRow` 抽成公共组件，Dock 标签页也改用它，避免两套实现漂移）：
    - 已授权 → 绿色 `checkmark.circle.fill`「辅助功能权限已开启」；
    - 未授权 → 橙色 `exclamationmark.triangle.fill`「需要「辅助功能」权限才能接管 ⌘⇥ 切换」+ 两个按钮「打开系统设置授权」「重新检测」；
    - 授权后回到 app 自动重查（`NSApplication.didBecomeActiveNotification`）。
  - 同 Section 下方补充说明文字：「该权限与 Dock 单击最小化共用，若已开启则此处直接显示绿色。」
- **FR-21.6** 选项 Section（开关开启后显示）：
  - ~~`Toggle("显示其他 Space 的窗口", isOn: $store.windowSwitcherAllSpaces)`~~（**已删除**：切换器固定总是列出所有 Space 的窗口）；
  - `Toggle("显示无窗口的应用", isOn: $store.windowSwitcherShowWindowlessApps)`，默认**开**（见 FR-17.7）；
  - footer：`"开启「显示无窗口的应用」后，窗口全部关闭但仍在运行的应用也会显示（无缩略图时显示 app 图标）。"`
- **FR-21.7** 屏幕录制权限（开关开启后显示）：复用截图页的 `ScreenRecordingPermissionRow()`（实时状态：绿色=已授权；橙色=引导授权 + 重新检测）。无权限时缩略图回退 app 图标、但切换仍可用（见 FR-18.7）。

#### FR-22 权限汇总

| 权限 | 用途 | 无权限时表现 |
|---|---|---|
| 辅助功能（`AXIsProcessTrusted`） | 事件 tap 吞事件 + AX 激活窗口 | 模块静默不生效，设置页橙色引导；授权后自动生效 |
| 屏幕录制（`CGPreflightScreenCaptureAccess`） | 窗口标题 + 真实缩略图 | 缩略图回退 app 图标、标题回退 app 名，切换仍可用 |

#### FR-23 忽略名单 【预留，第一版不做】

- 预留「忽略的 app」名单设置（key：`cmdTabSwitcherIgnoredApps: [String]`，存 bundleID），被列入的 app 不出现在面板、不可被切到。与 AltTab 的 ExceptionsTab 对齐（AltTab: `src/switcher/state/ExceptionMatcher.swift`）。第一版不实现，仅保证架构不排斥后续加（WindowList 过滤处留扩展点）。

### 12.4 设置持久化（UserDefaults Key）

> 全部 key 已并入主文档 FR-12.9 表（含 `cmdTabSwitcherEnabled` / `windowSwitcherShowWindowlessApps` / `cmdTabShortcut.keyCode` / `cmdTabShortcut.modifiers`），此处不重复列出。~~`windowSwitcherAllSpaces` 已删除~~。

### 12.5 架构与文件清单

#### 12.5.1 新增文件（工程用 `PBXFileSystemSynchronizedRootGroup`，新 .swift 自动纳入编译，无需改 pbxproj）

| 文件 | 职责 | 对应 AltTab 参考 |
|---|---|---|
| `Zeroflow/Services/CommandTabSwitcher.swift` | 事件 tap + 会话状态机 + 启停/重试（结构仿 `DockClickMinimizer`） | `src/events/KeyboardEvents.swift`、`src/switcher/SwitcherSession.swift` |
| `Zeroflow/Services/WindowList.swift` | 窗口枚举 + 幽灵过滤 + 窗口级 MRU 排序 | `src/windowserver/WindowServerQuery.swift`、`src/switcher/state/Window.swift` |
| `Zeroflow/Services/CGSWindowServer.swift` | SkyLight/CGS 私有 API dlsym 桥（SLS 批量枚举 + 成员列表 + Space 拓扑），带公开 API 兜底 | `src/macos/CGSCallScheduler.swift`、`src/macos/api-wrappers/SkyLight.framework.swift` |
| `Zeroflow/Services/PhantomWindowDetector.swift` | 幽灵窗口判定纯函数（对齐 AltTab cgsVerdict） | `src/switcher/state/PhantomWindowDetector.swift` |
| `Zeroflow/Services/WindowActivityTracker.swift` | 窗口级 MRU：AX `kAXFocusedWindowChangedNotification` 焦点观察 + 激活后同步补记 | `src/windowserver/ActivationFocusResolver.swift` 等 lastActivityTime 机制 |
| `Zeroflow/Services/WindowThumbnailer.swift` | SkyLight `dlsym` 桥接 + 后台抓图 + 缓存/节流/降级 | `src/events/WindowCaptureEvents.swift`（`WindowCaptureScreenshotsPrivateApi`）、`src/switcher/state/WindowThumbnails.swift` |
| `Zeroflow/Services/WindowActivator.swift` | AX 还原/前置/激活/匹配窗口 + 本 app 主线程激活 | `src/switcher/state/Window.swift` focus() |
| `Zeroflow/Services/AXWindow.swift` | 共享 CGWindowID→AXUIElement 匹配（`_AXUIElementGetWindow` + bounds 兜底） + subrole 读取 | `src/windowserver/WindowElementAcquisition.swift` |
| `Zeroflow/Services/WindowOps.swift` | 卡片操作：退出/关闭/最小化/全屏（本 app 主线程 NSWindow、其他 app 后台 AX） | `src/state/WindowOps.swift` |
| `Zeroflow/Views/WindowSwitcherPanel.swift` | NSPanel + SwiftUI 瓦片网格（含 `WindowSwitcherPanelView`） | `src/switcher/main-window/TilesPanel.swift`、`TilesView.swift`（简化版） |
| `Zeroflow/Views/AccessibilityPermissionRow.swift` | 由 `DockPermissionRow` 抽出的公共辅助功能权限行 | —（本仓库内复用 `Zeroflow/Views/SettingsView.swift`） |

#### 12.5.2 修改文件

| 文件 | 改动 |
|---|---|
| `Zeroflow/Models/SettingsStore.swift` | 新增 `cmdTabSwitcherEnabled`、`windowSwitcherShowWindowlessApps`、`cmdTabShortcut`（Key + `@Published` + defaults 注册 + 通知：开关变化发 `CommandTabSwitcher.didChangeNotification`、快捷键变化发 `CommandTabSwitcher.shortcutDidChangeNotification`；加载时把非两选一旧值钳回默认） |
| `Zeroflow/Views/ShortcutRecorderView.swift` | 泛化：注入 `Binding<ShortcutKey>` + `onSuspend/onResume` 闭包；**仅截图页使用**（「切换」页改为固定 Picker 二选一，见 FR-21.4） |
| `Zeroflow/Views/SettingsView.swift` | `SettingsTab` 加 `.switcher`；新增 `switcherTab`；`DockPermissionRow` 改用公共 `AccessibilityPermissionRow` |
| `Zeroflow/MenuBar/MenuBarController.swift` | `applicationDidFinishLaunching` 中调用 `CommandTabSwitcher.shared.reapply()` |
| `AGENTS.md` | 补充模块一句话说明 + 调试环境变量表加 `ZEROFLOW_SWITCHER_DEBUG=1` |
| `需求文档.md` / `README.md` | FR-12.9 Key 表、新增 FR 章节、README 功能列表 |

#### 12.5.3 启动/接线时序

```
MenuBarController.applicationDidFinishLaunching
  └─ CommandTabSwitcher.shared.reapply()
       ├─ 读取 cmdTabSwitcherEnabled + cmdTabShortcut
       ├─ ON  → 后台线程装 tap（2s 周期重试直至授权）
       └─ OFF → 卸 tap
CommandTabSwitcher.didChangeNotification（开关联动）
  └─ reapply()
CommandTabSwitcher.shortcutDidChangeNotification（快捷键联动）
  └─ 会话未激活时重读快捷键；会话激活时不打断（下次生效）
```

#### 12.5.4 调试辅助环境变量

| 变量 | 作用 |
|---|---|
| `ZEROFLOW_SWITCHER_DEBUG=1` | 启动后 dump 一次窗口列表（数量、app、是否含最小化/其他 space 窗口）到 ZSLog，并在日志打印一次「⌘⇥ tap 是否已安装」；事件 tap 对每次 key 事件的 keyCode/flags/反向判定打日志 |

### 12.6 边界场景与异常处理

1. **未授权辅助功能** → tap 不装、模块静默、设置页橙色引导；授权返回自动生效，无需重启。
2. **未授权屏幕录制** → 面板仍可用，缩略图回退 app 图标、标题回退 app 名。
3. **最小化窗口** → 列表中保留；缩略图走私有 API 可抓；选中后先 AX 还原再激活；还原动画期不重抓。
4. **其他 Space 窗口** → 总是列出；选中后由 activate+raise 自动切 Space。
5. **Secure Input（密码框）** → keyDown tap 被系统过滤，可能回退系统切换（已知限制，不强制解决）。
6. **私有 SkyLight API 失效** → 抓图全部失败时降级为图标卡；列表/切换不受影响；`ZSLog` 记录。
7. **tap 被系统暂停/超时** → 重新 `tapEnable` 恢复；tap 创建失败每 2s 重试。
8. **与截图/Dock 模块共存** → 三套事件机制互不冲突；会话互斥只在本模块内（会话进行中重复 ⌘⇥ 视为前进）。
9. **面板打开时 app 被关闭/结束** → 目标 app 退出则丢弃该会话（`applicationWillTerminate` 时清理 tap，参照 AltTab `applicationWillTerminate` 还原系统热键的精神，本模块 tap 随进程退出自然消失）。
10. **全屏 Space 下面板偶发不显示** → 面板弹起后复检 `isVisible`，不可见则延迟重新 `orderFront`。
11. **快速连按 ⌘⇥ 顺序错乱** → 激活目标窗口时同步补记 MRU（见 FR-17.4），保证下次枚举 index0=刚激活窗口。

### 12.7 验收标准

1. 设置「切换」标签页有独立开关 + 权限引导按钮（绿/橙两态正确）+ 快捷键录制器（默认显示 `⌘⇥`）。
2. 开启后，在任意 app 按 ⌘⇥：出现缩略图面板，**系统切换器完全不出现**。
3. ⇥ 前进（末尾环绕回开头）/ ⇧⇥ 后退（开头环绕回末尾）/ 按住连发 / Esc 取消正确；松开 ⌘ 立即切到选中的窗口。
4. 把快捷键改为 ⌥⇥ 并保存 → 立即生效：按 ⌥⇥ 弹面板、⌥⇥/⌥⇧⇥ 前后退、松 ⌥ 切换；⌘⇥ 恢复系统默认行为；「恢复默认」可回到 ⌘⇥。
5. 同 app 开 2 个窗口、最小化 1 个窗口、⌘H 一个 app、其他 Space 放 1 个窗口 → 均出现在面板并可被切到（最小化窗口切后自动还原）；无窗口后台 app 按开关显示占位卡。
6. 缩略图真实显示各窗口画面；最小化窗口也有缩略图；个别窗口抓不到时显示 app 图标。
7. 鼠标悬停高亮、单击即切换；悬停某操作按钮该按钮微放大；多屏下面板居中于鼠标所在屏。
8. 全屏窗口下按 ⌘⇥ 面板稳定显示，可切换出去。
9. 快速连按 ⌘⇥ 多次后卡片顺序正确（刚激活窗口在首位，上一个在次位，以此类推）。
10. 关闭总开关 → tap 移除、系统 ⌘⇥ 完全恢复、系列行为如常。
11. 无辅助功能权限时：开关可打开、模块静默、仅设置页引导，不崩溃。
12. `xcodebuild` Debug 构建通过；`ZEROFLOW_SWITCHER_DEBUG=1` 日志可打。

### 12.8 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| S1 基础 | 数据模型/设置项/新标签页/事件 tap + 面板骨架 | ✅ 已实现 |
| S2 列表与缩略图 | WindowList + WindowThumbnailer（SkyLight）+ 缓存降级 | ✅ 已实现 |
| S3 切换与打磨 | WindowActivator + 悬停点击 + 多屏 + 调试探针 + 文档 | ✅ 已实现（本次打磨：循环切换 / ⇧⇥ 反向 / 全屏显示 / MRU 顺序 / 按钮悬停动画 / 删除 Space 过滤选项） |

### 12.9 附录：AltTab 具体引用清单（实现时对照）

| 目的 | AltTab 文件 |
|---|---|
| 事件 tap 创建、tap 失效恢复、Esc 吸收思路 | `src/events/KeyboardEvents.swift` |
| 会话生命周期（`SwitcherSession`） | `src/switcher/SwitcherSession.swift` |
| 窗口列表采集（我方可简化为公开 API） | `src/windowserver/WindowServerQuery.swift`、`src/windowserver/WsWindowState.swift` |
| 缩略图抓取（私有 API `CGSHWCaptureWindowList`） | `src/events/WindowCaptureEvents.swift` → `WindowCaptureScreenshotsPrivateApi` |
| 缩略图尺寸/节流/恢复动画延迟 | `src/switcher/state/WindowThumbnails.swift` |
| 激活/前置/最小化还原 | `src/switcher/state/Window.swift` → `func focus()` |
| 私有单行桥 `_AXUIElementGetWindow` 等 | `src/macos/api-wrappers/HIServices.framework.swift` |
| AX↔wid 匹配兜底 | `src/switcher/state/BruteForceWindowMatch.swift` |
| 面板 UI | `src/switcher/main-window/TilesPanel.swift`、`src/switcher/main-window/TilesView.swift` |
