# zeroflow — macOS 原生截屏软件需求文档

> 版本：v1.1
> 状态：M1/M1.5/M2 及 Dock 模块已实现，M3 体验打磨中

---

## 1. 项目概述

### 1.1 产品定位
一款**极简、轻量、启动迅速**的 macOS 原生截图工具。用户按快捷键即可随时截图，截图后可对图片进行轻量标注编辑（画线、矩形/圆框标注、文字气泡、马赛克涂抹），并支持一键下载到本地或复制到剪贴板。

### 1.2 产品名称
zeroflow（曾用名 Zeroshot）

### 1.3 技术栈
| 项 | 选型 |
|---|---|
| 语言 | Swift（SWIFT_VERSION 5.0） |
| UI 框架 | SwiftUI + AppKit |
| 屏幕捕获 | ScreenCaptureKit（`SCScreenshotManager`，macOS 14+） |
| 全局热键 | Carbon `RegisterEventHotKey` |
| 开机自启 | ServiceManagement `SMAppService.mainApp` |
| Dock 单击最小化 | CGEventTap + AXUIElement |
| 持久化 | UserDefaults（统一切口 `SettingsStore`） |
| 最小系统 | macOS 14.0（Sonoma）及以上 |
| 架构 | 单工程、无第三方依赖、无 Package |

### 1.4 设计目标（非功能需求，详见第 6 章）
- **轻量**：安装包 ≤ 5MB，常驻内存 ≤ 80MB
- **启动迅速**：冷启动到菜单栏就绪 ≤ 0.5s；从按快捷键到进入选区模式 ≤ 0.3s
- 无侵入：常驻时无 Dock 图标（`LSUIElement = YES`），仅菜单栏图标
- 完全本地运行：无广告、无登录、无联网

### 1.5 目标用户
需要快捷截图并进行简单标注的 macOS 用户，偏好极简体验。

### 1.6 术语定义
| 术语 | 含义 |
|---|---|
| 选区模式 | 屏幕被遮罩覆盖、鼠标为十字光标、可拖拽框选的状态 |
| 编辑页 | 截图完成后弹出的编辑窗口，含图片展示与工具栏 |
| 气泡标注 | 带小尾巴的对话气泡样式文本框，指向用户点击的位置（锚点） |
| 驻留 | App 常驻菜单栏、无 Dock 图标运行的状态 |

---

## 2. 整体工作流程

```
开机 → App 自动启动（进程常驻，仅菜单栏图标，无 Dock 图标）
     │
     ├─ 用户按快捷键 ⌘⇧S（默认，可自定义）或菜单栏「立即截图」
     │     ↓（截图功能总开关关闭时忽略本次触发）
     │     ↓ 未授权屏幕录制 → 自动打开设置页「截图」标签页展示权限引导
     │     ↓ 已授权 → 进入选区模式：十字光标 + 每个显示器一个全屏遮罩窗
     │     ↓ 拖拽选区（实时显示 宽x高）→ 松开 → 捕获 → 无缝进入编辑页
     │     ↓ 编辑：画线 / 矩形标注 / 圆框标注 / 文字气泡 / 马赛克 + 撤销/重做
     │     ↓ 下载到本地 / 复制到剪贴板（不自动关窗）
     │
     ├─ 设置页（通用 / 截图 / Dock 三个标签页）
     │
     └─ Dock 单击最小化模块（设置页开关开启时生效，后台无感运行）
```

---

## 3. 功能需求

> 优先级：P0 = 已实现（MVP+），P1 = 第二优先级，P2 = 可选。

### FR-1 全局快捷键唤起截图 【P0 已实现】

- **FR-1.1** 默认快捷键：`Shift + Command + S`（`⌘⇧S`）。
- **FR-1.2** 使用 Carbon 全局热键注册，当 App 在前台/后台、任意其他 App 活动时均能触发。
- **FR-1.3** 同一时刻只能有一个截屏会话：若已处于选区模式，再次触发快捷键时忽略（`CaptureCoordinator.isActive` 防重入）。
- **FR-1.4** 触发时聚焦鼠标当前所在的显示器进行截屏。
- **FR-1.5** 截图功能总开关（`screenshotEnabled`）关闭时，全局热键不注册、触发被忽略。
- **验收**：任意应用中按下 `⌘⇧S`，在 0.3s 内出现遮罩与十字光标。

### FR-2 选区交互 【P0 已实现】

- **FR-2.1** 遮罩层：每个显示器一个无边框全屏透明窗口（`NSPanel` 风格，`windowLevel = .screenSaver`，`canJoinAllSpaces`），半透明黑色遮罩（不透明度 0.35）。
- **FR-2.2** 光标：进入截屏模式的瞬间，光标变为十字准星。
- **FR-2.3** 拖拽选择：按住鼠标左键拖拽形成矩形选区；选区外加深遮罩、选区内 clear 显示原图、蓝色边框。
- **FR-2.4** 尺寸提示：拖拽过程中，选区边缘实时显示 `宽x高`（如 `1230×680`），跟随鼠标位置。
- **FR-2.5** Esc 取消：按 `Esc` 取消本次截图，回到菜单栏驻留，无残留遮罩。
- **FR-2.6** 高 DPI：Retina 屏幕按物理像素（`backingScaleFactor`）截取与保存，尺寸提示使用物理像素。
- **FR-2.7** 原地单击（拖拽 <1px）视为截取全屏；仅单边 ≥1px 的选区视为无效，按取消处理。
- **FR-2.8** 把光标放置到鼠标所在显示器对应的遮罩窗口并使其成为 key 窗口（可接收 Esc）。
- **验收**：在单屏/多屏、系统缩放 100% / 125% / 200% 下选区坐标换算正确，尺寸提示与实际截图像素一致。

### FR-3 编辑页窗口 【P0 已实现】

- **FR-3.1** 选区完成后立即弹出编辑窗口。
- **FR-3.2** 窗口构成：中间是截取的原始图片（等比居中、只缩小不放大，保证像素清晰），下方一条工具栏。
- **FR-3.3** 窗口带标题栏（标题「zeroflow 截图」）、可关闭、可缩放；初始尺寸按截图自适应（上限约 1100×760，下限 720×300 保证工具栏完整显示）。
- **FR-3.4** 关闭方式：Esc、关闭按钮、⌘W；下载/复制不自动关窗。

### FR-4 工具栏总览 【P0 已实现】

- **FR-4.1** 位置：编辑页底部水平排列。
- **FR-4.2** 按钮按用途分组（撤销/重做 → 绘制工具 → 标注 → 马赛克 → 下载/复制），固定顺序见下表。
- **FR-4.3** 按钮使用极简线性图标（SF Symbols），选中态以高亮底色/描边区分。
- **FR-4.4** 鼠标悬停时显示按钮名称 tooltip（含快捷键提示）。
- **FR-4.5** 全部操作均有快捷键（绘制类工具除外），且仅当编辑页窗口获得焦点时生效。

| 顺序 | 按钮 | 图标（SF Symbols 参考） | 悬停提示 | 快捷键 |
|---|---|---|---|---|
| 1 | 撤回 | `arrow.uturn.backward` | 撤回 (⌘Z) | `⌘Z` |
| 2 | 重做 | `arrow.uturn.forward` | 重做 (⇧⌘Z) | `⇧⌘Z` |
| 3 | 画线 | `pencil` | 画线 (⌘L) | `⌘L` |
| 4 | 矩形标注 | `rectangle` | 矩形标注 | — |
| 5 | 圆框标注 | `circle` | 圆框标注 | — |
| 6 | 画笔颜色 | 自绘色圆 | 画笔颜色 | — |
| 7 | 粗细 | 自绘线宽 | 粗细 | — |
| 8 | 标注 | `text.bubble.fill` | 标注 (T) | `T` |
| 9 | 马赛克 小/中/大 | 自绘像素图标 | 马赛克 小/中/大 | — |
| 10 | 下载 | `arrow.down.to.line` | 下载保存 (⌘S) | `⌘S` |
| 11 | 复制 | `doc.on.doc` | 复制 (⌘C) | `⌘C` |

- **FR-4.6** 画笔颜色与粗细仅在画线/矩形/圆框工具激活时可操作（其他工具下置灰）。
- **FR-4.7** 撤销/重做按钮在不可用（栈空）时置灰。

### FR-5 撤回 / 重做 【P0 已实现】

- **FR-5.1** 可撤销操作：画线、矩形/圆框、文字气泡、马赛克的创建；气泡/形状的移动、缩放、删除；气泡文字修改。
- **FR-5.2** 无可撤销操作时「撤回」按钮置灰禁用；可撤销时高亮可用。
- **FR-5.3** 重做：撤销后即时可重做；一旦发生新的编辑操作，重做栈清空。
- **FR-5.4** 历史栈上限 50 步，超出丢弃最早步骤。
- **FR-5.5** 支持工具栏按钮与键盘快捷键（`⌘Z` / `⇧⌘Z`）两种触发方式。
- **验收**：连续执行多种编辑共 10 次操作后多次撤销再重做，最终画面与操作前像素一致。

### FR-6 画线 【P0 已实现】

- **FR-6.1** 点击「画线」按钮或按 `⌘L` 进入画线模式；再次点击按钮切回选择。
- **FR-6.2** 按住鼠标左键在图上自由绘制（连续点集 + 圆角线段实时渲染）。
- **FR-6.3** 松开左键结束本条线，成为独立对象（可整体被撤销；当前版本画线不支持选中/移动/删除）。
- **FR-6.4** 颜色与粗细在工具栏内直接操作（无浮动面板）：
  - **颜色**：10 个预设色（红/橙/黄/绿/蓝/紫/粉/灰/黑/白），点击色圆弹出预设色板；默认红色。
  - **粗细**：弹出面板选 细/中/粗（2 / 5 / 9px），默认 3px。
- **FR-6.5** 线条平滑、抗锯齿；绘制过程中实时更新渲染。
- **FR-6.6** 按 `Esc` 关闭编辑页（无单独「退出画线模式」快捷键，点按钮或 `⌘L` 切换工具）。
- **验收**：选择红色、粗细 5 后绘制，线条颜色/粗细与预设一致，且可撤销。

### FR-7 矩形 / 圆框标注 【P0 已实现】

- **FR-7.1** 点击「矩形标注」/「圆框标注」按钮进入对应绘制模式。
- **FR-7.2** 拖拽生成仅描边、中间透明的矩形 / 圆框；颜色与线宽跟随画笔设置。
- **FR-7.3** 松开左键提交，成为独立可选中对象：单击选中显示四角缩放控点，可拖动移动、拖角缩放、`Backspace`/`Delete` 删除。
- **FR-7.4** 绘制完成后自动切回选择工具。

### FR-8 文字气泡标注 【P0 已实现】

- **FR-8.1** 点击「标注」按钮或按 `T`（再次按 `T` 切回选择）进入标注模式。
- **FR-8.2** 点击图片任意位置，生成一个气泡：圆角白色矩形 + 黑色文字 + 一条指向锚点的引线 + 锚点圆点，并**立即进入文字编辑**（白底文本框内闪烁光标等待输入）。
- **FR-8.3** 文本自动换行、气泡尺寸随文字自适应（宽上限 240px、高上限 320px，锚定右上角生长）。
- **FR-8.4** 气泡固定白色底 / 黑色文字（mvp 不支持改气泡颜色）。
- **FR-8.5** 气泡编辑交互：
  - **双击**气泡重新进入文字编辑；
  - 单击选中；拖拽移动（整框 + 锚点一起动）、拖四角缩放、拖锚点圆点单独移动引线起点；
  - `Backspace` / `Delete` 删除选中气泡；
  - 点击气泡外空白处或按 `Esc` 提交文字并退出编辑。
- **FR-8.6** 气泡是独立图层对象，可被单条撤销（`⌘Z`）。
- **验收**：点击并输入文字，生成带尾巴的气泡，可移动、缩放、重编辑、删除、撤销。

### FR-9 马赛克涂抹 【P0 已实现】

- **FR-9.1** 点击「马赛克 小/中/大」按钮进入马赛克模式（笔刷半径 12 / 18 / 28px）。
- **FR-9.2** 按住鼠标左键在图上涂抹，涂抹区域实时显示为马赛克（整图像素化后按圆形笔刷裁剪）。
- **FR-9.3** 松开左键提交为独立对象（可整体撤销；当前版本马赛克不支持选中/移动）。
- **FR-9.4** 预览、提交、导出三者的马赛克样式完全一致（同一像素化叠加图按半径缓存复用）。
- **验收**：涂抹区域像素块与预览一致，导出图片同效果，可撤销。

### FR-10 下载保存 【P0 已实现】

- **FR-10.1** 点击「下载」或按 `⌘S`，将**当前编辑后的完整图片**（按原图分辨率合成）保存到本地。
- **FR-10.2** 保存行为受设置控制，二选一：
  - 模式 A（默认）：直接保存到「默认保存位置」。
  - 模式 B：设置开启「保存时询问位置」后，每次弹出系统保存面板（`NSSavePanel`）供用户选择路径与文件名。
- **FR-10.3** 文件名规则：`zeroflow_yyyyMMdd_HHmmss.png`（如 `zeroflow_20260808_153012.png`）；同目录重名自动追加 `-1`、`-2`…避免覆盖。
- **FR-10.4** 保存格式：PNG（无损）。
- **FR-10.5** 保存成功后记录最近保存路径（供菜单「打开最近截图」使用），**不自动关闭编辑页**；保存失败弹系统警告框。
- **验收**：默认模式保存到配置目录；开启询问后每次弹面板；连续保存文件名不重复。

### FR-11 复制到剪贴板 【P0 已实现】

- **FR-11.1** 点击「复制」或按 `⌘C`，将编辑后的完整图片（PNG 数据）写入系统剪贴板（`NSPasteboard`，类型 `png`）。
- **FR-11.2** 复制结果可粘贴到任意支持图片的 App（微信、iMessage、Pages、预览等）。
- **FR-11.3** 复制后**不自动关闭编辑页**。
- **验收**：复制后直接打开「预览」粘贴可得到完整编辑后的图片。

### FR-12 设置页 【P0 已实现】

设置页是本软件独立的配置中心，所有配置项分组展示、修改后**立即生效并即时持久化**（无「保存」按钮，无需重启）。

- **FR-12.0.1** 入口：菜单栏图标 →「设置…」（`⌘,`）；设置窗口为单例（重复打开只聚焦已存在窗口，不重复创建）。
- **FR-12.0.2** 窗口规格：约 560×640 pt，`titled + closable`，居中显示于主屏。
- **FR-12.0.3** 所有设置修改后立即生效并即时持久化。
- **FR-12.0.4** 持久化：统一走 `SettingsStore`（`UserDefaults`）；存储 key 见 FR-12.9。
- **FR-12.0.5** 关闭窗口不丢失任何已修改项；重新打开时显示当前持久化值。
- **FR-12.0.6** 顶部分段控件切换三个标签页：
  ```
  设置
   ├── 通用
   │     └── 开机自动启动（开关）
   ├── 截图
   │     ├── 启用截图功能（总开关）
   │     ├── 截屏快捷键（按键录制框 + 恢复默认）
   │     ├── 屏幕录制权限（绿色=已授权 / 橙色警告 + 授权/重检按钮，开启截图后显示）
   │     └── 保存
   │           ├── 默认保存位置（路径 + 选择… + 打开）
   │           └── 保存时询问位置（开关）
   └── Dock
         ├── 单击 Dock 图标最小化窗口（开关）
         ├── 辅助功能权限引导（开启后显示）
         └── 「桌面与 Dock」前往设置 + 提示
  ```

#### FR-12.1 截图功能总开关
- **FR-12.1.1** 「启用截图功能」开关（key：`screenshotEnabled`，默认**关**）。
- **FR-12.1.2** 关闭时：全局热键不注册、菜单「立即截图」置灰、触发截图被忽略。
- **FR-12.1.3** 开启时若未授权屏幕录制，自动弹出系统授权（`CGRequestScreenCaptureAccess`）。

#### FR-12.2 截屏快捷键设置
- **FR-12.2.1** 展示当前快捷键，形式为一个可点击的「按键录制框」，显示形如 `⇧⌘S` 的组合。
- **FR-12.2.2** 点击按键录制框进入「录制」状态（框高亮 + 提示「按下新快捷键…」），等待用户按键；`Esc` 取消录制。
- **FR-12.2.3** 校验规则：必须包含至少一个修饰键（⌘/⌃/⌥/⇧）且主键为非修饰键，否则提示「请至少按下一个修饰键」并继续录制。
- **FR-12.2.4** 录制期间暂停全局热键（避免按下相同组合触发截图），录制结束恢复。
- **FR-12.2.5** 与当前设置相同 → 直接退出录制。
- **FR-12.2.6** 冲突处理：新快捷键注册失败（被系统或其他 App 占用）时热键不生效、旧热键仍保留，不崩溃；用户可再次录制修改。
- **FR-12.2.7** 「恢复默认」按钮：一键恢复 `⌘⇧S` 并重新注册。
- **FR-12.2.8** 保存 key：`globalShortcut.keyCode` + `globalShortcut.modifiers`。
- **FR-12.2.9** 修改后立即更新全局热键（`GlobalHotkeyManager` 监听 `zeroflow.shortcutDidChange` 通知自动重注册）。

#### FR-12.3 开机自启
- **FR-12.3.1** 「开机自动启动（登录时打开）」开关（key：`launchAtLogin`，默认**开**）。
- **FR-12.3.2** 通过 `SMAppService.mainApp`（macOS 13+）注册/注销 LaunchAgent。
- **FR-12.3.3** 启动时自动把持久化开关值与系统登录项实际注册状态对齐（修复「默认开但重启不自动启动」）。
- **FR-12.3.4** 注册失败自动回滚开关。
- **验收**：开关打开后，系统「登录项」中包含本 App；重启后自动运行。

#### FR-12.4 默认保存位置
- **FR-12.4.1** 一行显示「默认保存位置」+ 当前路径（截断显示，可复制），右侧按钮：`选择…`（`NSOpenPanel` 选目录）、`打开`（Finder 打开）。
- **FR-12.4.2** 默认值：`~/Downloads/zeroflow`；首次使用或读取缺失时自动写入并创建目录（`FileManager.createDirectory`）。
- **FR-12.4.3** 用户选择的路径写入设置（key：`saveDirectory`）；不存在则尝试创建，失败提示。
- **验收**：默认返回 `~/Downloads/zeroflow`；选择新目录后立即更新、且下载功能使用新目录。

#### FR-12.5 保存询问策略
- **FR-12.5.1** 「保存时询问位置」开关（key：`askSaveLocation`，默认**关**）。
- **FR-12.5.2** 关：每次保存直接存到默认目录；开：每次弹 `NSSavePanel`。
- **验收**：切换开关后，立即按「下载」验证行为变化正确。

#### FR-12.6 Dock 单击最小化开关
- **FR-12.6.1** 「单击 Dock 图标最小化窗口」开关（key：`dockClickMinimize`，默认**关**）。
- **FR-12.6.2** 开启后立即生效并持久化；关闭后完全恢复系统默认行为（移除事件监听）。
- **FR-12.6.3** 开启时若未授权辅助功能，自动弹出系统授权（`AXIsProcessTrustedWithOptions`）。
- **FR-12.6.4** 开启后显示辅助功能权限状态行（绿=已授权 / 橙=引导授权）与「桌面与 Dock」前往设置按钮（建议开启「最小化窗口到应用图标」）。

#### FR-12.7 系统权限指引
- **FR-12.7.1** 截屏权限引导内联在设置页「截图」标签页：已授权显示绿色「屏幕录制权限已开启」，未授权显示橙色警告 + 「打开系统设置授权 / 重新检测」按钮。
- **FR-12.7.2** 授权后回到 App 自动重查（真实探测，避免 preflight 缓存误判）。
- **FR-12.7.3** 未授权时触发截图：不弹窗，自动打开设置窗口并切换到「截图」标签页展示权限引导；禁止进入选区模式（避免产生全黑截图）。

#### FR-12.8 工具栏默认属性（预留分组，P1 未实现）
- 预留「默认画笔颜色」「默认画笔粗细」设置项，P1 优先级，当前版本未实现。

#### FR-12.9 设置数据持久化（UserDefaults Key 汇总）
| Key | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `globalShortcut.keyCode` | Int | 1 (s) | 全局截屏快捷键键码 |
| `globalShortcut.modifiers` | Int | cmd+shift | 全局截屏快捷键修饰键 |
| `launchAtLogin` | Bool | true | 开机自启 |
| `saveDirectory` | String | `~/Downloads/zeroflow` | 默认保存目录 |
| `askSaveLocation` | Bool | false | 保存时是否询问位置 |
| `screenshotEnabled` | Bool | false | 截图功能总开关 |
| `dockClickMinimize` | Bool | false | 单击 Dock 图标最小化窗口 |
| `lastSavedPath` | String | nil | 最近保存路径（菜单「打开最近截图」用） |

### FR-13 菜单栏驻留 【P0 已实现】

- **FR-13.1** 常驻 `NSStatusItem` 菜单栏，图标为 `bolt.fill`。
- **FR-13.2** 菜单项（自上而下）：
  - `立即截图（⌘⇧S）` → 进入截屏模式（显示当前快捷键；截图总开关关闭时置灰）
  - `设置…（⌘,）`
  - `打开最近截图` → Finder 打开最近保存的图片（仅当存在最近保存记录时显示）
  - `退出 zeroflow（⌘Q）`
- **FR-13.3** 无 Dock 图标（`LSUIElement = YES`）。

### FR-14 单击 Dock 图标最小化窗口 【P0 已实现】

- **FR-14.1** 能力：单击 Dock 上「当前前台已激活 app」的图标，最小化该 app 的**聚焦窗口**（最近操作的窗口；取不到回退主窗口）。恢复窗口完全交给 macOS 原生行为（再点一次 Dock 图标即恢复），本模块只做「最小化」这一个方向。
- **FR-14.2** 判定：被点击 app == 当前前台 app（`NSWorkspace.frontmostApplication`），且该 app 在当前空间存在**可见、非全屏、未最小化**的窗口。
- **FR-14.3** 场景：
  - 单击非前台 app / 全屏中 app / Launchpad、废纸篓等无 app 身份项 / option、control 点击 → 放行给系统，不干预。
  - 该 app 窗口已全部最小化 → 放行给系统恢复。
- **FR-14.4** 实现：`CGEventTap`（`cghidEventTap` + 左键按下）识别 Dock 图标（AX dock item frame 缓存快路径 + `AXUIElementCopyElementAtPosition` 兜底命中），成功后吞掉事件（避免「再点即恢复」抵消最小化），最小化动作异步执行（事件回调内只识别 + 吞事件）。
- **FR-14.5** 多显示器 / 多 Space：只处理落在当前屏幕、当前空间的可见非全屏窗口；其他一律不动。
- **FR-14.6** 依赖辅助功能权限：未授权时开关仍可开但模块静默跳过，设置页展示引导；授权返回后自动生效，无需重启。
- **FR-14.7** 事件 tap 失效（权限被收回/系统暂停）时每 2s 周期自动重建，不崩溃。
- **验收**：开启开关后，任意前台 app 单击其 Dock 图标 → 聚焦窗口滑入 Dock；多窗口 app 只收走一个窗口；再点一次恢复；关闭开关后完全恢复系统默认。

---

## 4. 数据模型与图像处理逻辑（实现级参考）

### 4.1 文档模型
- `EditDocument`：`image`（`NSImage` 原始截图）+ `elements`（`[CanvasElement]` 编辑对象列表）+ 撤销/重做双栈。

### 4.2 CanvasElement 统一类型（4 种）
| 类型 | 字段 | 可否选中 |
|---|---|---|
| `.stroke` | 点集 `[CGPoint]`、颜色 `Color`、线宽 `CGFloat` | 否（可整体撤销） |
| `.shape` | 框 `CGRect`、样式 `rect/ellipse`、颜色、线宽 | 是（移动/缩放/删除） |
| `.bubble` | 锚点、文本框 `CGRect`、文字 | 是（移动/缩放/锚点/删除/双击编辑） |
| `.mosaic` | 点集 `[CGPoint]`、半径 `CGFloat` | 否（可整体撤销） |

### 4.3 渲染
- 在原始截图上按顺序重绘所有 `CanvasElement`，合成最终 `NSImage`；
- 下载 / 复制均使用该合成图（原图物理像素分辨率）；
- 马赛克渲染：整图最近邻像素化叠加图按半径缓存（`MosaicOverlayCache`），按圆形笔刷裁剪，预览/提交/导出共用同一张图。

### 4.4 撤销栈
- 双栈实现（undo/redo），容量上限 50；任何修改前先 `snapshotBeforeChange()`。

### 4.5 多屏坐标
- 每个屏幕一个遮罩窗口，选区矩形在该屏幕坐标系内（左下原点）；
- ScreenCaptureKit `sourceRect` 为左上原点 → `ScreenCapture.topLeftRect` 负责换算；
- Retina 按 `backingScaleFactor` 换算物理像素。

---

## 5. 界面与交互规范

| 规范 | 说明 |
|---|---|
| 图标风格 | 线性图标（SF Symbols），选中/未选中状态区分（高亮底色 + 描边） |
| 工具栏样式 | 系统控制背景色（`controlBackgroundColor`），顶部细分隔线，高约 28pt |
| 悬停反馈 | tooltip 显示按钮名称与快捷键 |
| 交互反馈 | 全部反馈 100ms 内响应，无加载动画 |
| 窗口 | 编辑页带标题栏、可缩放，初始尺寸按截图自适应 |
| 主题 | 跟随系统深色/浅色切换 |
| 语言 | 界面文本简体中文 |
| 无障碍 | 按钮提供 `.help` tooltip |

---

## 6. 性能与质量目标

| 指标 | 目标 |
|---|---|
| 启动时间 | 冷启动到菜单栏就绪 ≤ 0.5s；快捷键到选区 ≤ 0.3s |
| 内存占用 | 驻留 ≤ 80MB，空闲无后台轮询定时器 |
| 截图延迟 | 松开鼠标到编辑页可见 ≤ 300ms |
| 选区渲染 | 拖拽 resize 实时渲染无卡顿（60fps 目标） |
| 马赛克 | 像素化叠加图按半径缓存，涂抹实时不卡顿 |
| 稳定性 | 选区/绘制过程中不允许未捕获异常；异常记录日志（`/tmp/zeroflow.log`） |
| 磁盘 | 除用户主动保存的文件外，无残留缓存 |

---

## 7. 边缘场景与异常处理

1. **未授权屏幕录制** → 打开设置页「截图」标签页引导，禁止截取，不崩溃。
2. **多显示器且缩放比例不同** → 每屏一个遮罩窗口，选区按鼠标所在屏，尺寸换算按物理像素。
3. **原地单击（<1px）** → 截全屏；仅单边 ≥1px → 取消。
4. **快捷键被其他 App/输入法占用注册失败** → 热键不生效、旧热键保留，设置页可再修改。
5. **截图总开关关闭** → 热键不注册、菜单置灰、触发忽略。
6. **保存目录不可写** → 弹系统警告框提示。
7. **编辑页打开时再次触发截图** → 忽略本次触发（防重入），避免中断当前编辑。
8. **Dock 单击最小化未授权辅助功能** → 模块静默跳过，设置页引导。
9. **事件 tap 失效**（Dock 模块）→ 周期自动重建，不崩溃。

---

## 8. 兼容性

| 项 | 要求 |
|---|---|
| 系统版本 | macOS 14.0（Sonoma）及以上 |
| 显示 | 支持 Retina、缩放分辨率、多显示器 |
| 芯片 | Apple Silicon 与 Intel 均支持（通用二进制） |
| 语言 | 首版简体中文 |

---

## 9. 里程碑规划

| 阶段 | 内容 | 状态 |
|---|---|---|
| **M1（MVP 基础）** | 菜单栏驻留、`⌘⇧S` 选区、编辑页、撤销/重做、画线、文字气泡、下载/复制、权限引导 | ✅ 已实现 |
| **M1.5（扩展工具）** | 矩形/圆框标注、马赛克涂抹 | ✅ 已实现 |
| **M2（全链路）** | 全局热键真接入、设置项全链路生效、开机自启修复 | ✅ 已实现 |
| **Dock 模块** | 单击 Dock 图标最小化窗口 + 辅助功能引导 | ✅ 已实现 |
| **M3（体验打磨）** | Toast、视觉规范、多屏深测、Developer ID 签名与 Notarization | ⏳ 部分完成，待公证 |
| **M4（可选）** | 长截图、贴图置顶、图形（箭头）等扩展 | 未开始 |

---

## 10. 验收标准总纲

1. 默认在任何 App 中按 `⌘⇧S`，立即进入十字光标截图；选区、尺寸提示、Retina 尺寸均正确。
2. 可绘制任意颜色/粗细的线、矩形、圆框、马赛克，并可在画布上撤销/重做而不丢失内容。
3. 可添加文字气泡并移动、缩放、重新编辑、删除、撤销，无需重启。
4. 下载覆盖两种模式：固定默认路径 / 每次询问路径；复制后粘贴到其他 App 有效。
5. 开启开机自启后，登录后图标驻留菜单栏，快捷键可正常截图。
6. 开启「单击 Dock 图标最小化窗口」后，单击前台 app 的 Dock 图标只最小化其聚焦窗口。
7. 未授权屏幕录制/辅助功能时有正确引导且不崩溃。

---

## 11. 发布与分发

- 应用标识：`com.zeroflow.app`。
- 首次 Release 需通过 Developer ID 签名 + Notarization，提供 DMG 与 `brew` 安装方式。
- 需求追踪：FR-x.y 编号与验收条目一一对应，便于回归测试。
