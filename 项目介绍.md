# zeroshot — 项目介绍（面向 AI 开发者）

> 用途：本文件面向 AI / 新开发者，说明项目做什么、代码结构如何组织、如何编译与运行。
> 完整功能与验收标准见同目录 `需求文档.md`（含 FR-x 编号，可逐条对照）。

---

## 1. 项目概览

**一句话定位**：zeroshot 是一款极简、轻量、启动迅速的 **macOS 原生截图工具**——常驻菜单栏（无 Dock 图标），按全局快捷键 `⌘⇧S` 框选截屏，截图后可在编辑页进行**画线 / 文字气泡标注**，支持**下载 PNG** 或**复制到剪贴板**。

**技术栈**

| 项 | 选型 |
|---|---|
| 语言 | Swift（SWIFT_VERSION 5.0） |
| UI 框架 | SwiftUI + AppKit（NSViewRepresentable、NSWindow） |
| 屏幕捕获 | ScreenCaptureKit（`SCScreenshotManager`，macOS 14+） |
| 全局热键 | Carbon `RegisterEventHotKey` |
| 开机自启 | ServiceManagement `SMAppService.mainApp` |
| 持久化 | `UserDefaults`（`@AppStorage` 风格，统一切口 `SettingsStore`） |
| 第三方依赖 | **无**（单工程、无 Package） |
| 最小系统 | macOS 14.0（Sonoma）+ |
| 签名 | `CODE_SIGN_IDENTITY = "-"`（本地开发可无签名构建） |

**目录结构**

```
zeroshot/
├── 需求文档.md                    # 详细需求与验收标准（FR-1 ~ FR-12，M0/M1 里程碑）
├── 项目介绍.md                    # 本文件
├── Zeroshot.xcodeproj/           # Xcode 工程（objectVersion 77，Xcode 16+，文件系统同步组）
├── Zeroshot/                     # 全部源码
│   ├── ZeroshotApp.swift         # 程序入口：自建 NSApplication + MenuBarController
│   ├── MenuBar/
│   │   └── MenuBarController.swift
│   ├── Models/
│   │   ├── SettingsStore.swift   # UserDefaults 持久化 + 自启/保存目录逻辑
│   │   ├── ShortcutKey.swift     # 快捷键值模型与显示/校验
│   │   └── EditDocument.swift    # 编辑文档 + CanvasElement + 撤销/重做双栈
│   ├── Views/
│   │   ├── SettingsView.swift    # 设置页（表单分组）
│   │   ├── ShortcutRecorderView.swift  # 快捷键录制/校验/恢复默认
│   │   ├── SaveDirectoryRow.swift      # 保存路径行（选择/打开）
│   │   ├── CaptureOverlayView.swift    # 屏幕选区遮罩（NSView 绘制）
│   │   └── EditorView.swift      # 编辑页：画布 + 工具栏 + 导出（约 880 行，核心文件）
│   └── Services/
│       ├── GlobalHotkeyManager.swift  # Carbon 全局热键注册/重注册
│       ├── CaptureCoordinator.swift   # 截图流程协调器（权限→遮罩→捕获→编辑器）
│       ├── ScreenCapture.swift        # 权限检测 + ScreenCaptureKit 捕获
│       └── ZSLog.swift                # 统一日志（stderr + /tmp/zeroshot.log）
└── dist/                          # 构建产物副本：Zeroshot.app + 启动脚本
```

---

## 2. 核心功能与模块说明

### 2.1 程序入口与菜单栏驻留

- `ZeroshotApp.swift`：不使用 SwiftUI `App` 协议，而是 `@main` 中手动创建 `NSApplication` 并将 `MenuBarController` 设为 delegate 后 `app.run()`。
- `MenuBar/MenuBarController.swift`：
  - `NSApp.setActivationPolicy(.accessory)` → **无 Dock 图标**，仅菜单栏 `NSStatusItem`（图标 SF Symbol `camera.viewfinder`）。
  - 启动时把全局热键触发回调接到 `CaptureCoordinator.startCapture()`，并注册持久化的快捷键。
  - 菜单项：立即截图 / 设置（`⌘,`）/ 打开最近截图 / 退出。
  - 设置窗口为**单例**（`settingsWindowController` 复用，重复打开不新建）。

### 2.2 全局热键（Carbon）

`Services/GlobalHotkeyManager.swift`：
- 用 Carbon `RegisterEventHotKey` 注册（signature `"ZRST"`，id 1），事件回调切到主队列触发 `onTrigger`。
- 监听 `NotificationCenter` 的 `zeroshot.shortcutDidChange`，在 `SettingsStore.shortcut` 变化时自动重注册 → **设置里改快捷键立即生效**。
- `register()` 返回 Bool；被系统/其他 App 占用时注册失败（不崩溃，仍保留旧热键）。

### 2.3 截图流程（CaptureCoordinator 编排）

```
热键/菜单 → CaptureCoordinator.startCapture()
  ├─ isActive 防重入（同一时刻仅一个截屏会话）
  ├─ 真实权限检测 hasScreenCaptureAccess()（SCShareableContent.current）
  │     ├─ 未授权 → presentPermissionGuide()：引导弹窗 → 跳系统设置/重新检测
  │     └─ 已授权 → NSCursor.crosshair + 为每个 NSScreen 创建 CaptureOverlayWindow
  ├─ 用户在遮罩上拖拽选区（CaptureOverlayNSView：全屏半透明黑 0.35 遮罩、
  │     选区内 clear、蓝色边框、实时 宽x高 尺寸提示；Esc 取消；单击=截全屏）
  ├─ completeSelection() → ScreenCapture.capture(screen:rect:)（ScreenCaptureKit）
  └─ openEditor(image:) → 弹出 EditorView 编辑窗口（按截图尺寸自适应窗口大小）
```

### 2.4 屏幕捕获（ScreenCaptureKit）

`Services/ScreenCapture.swift`：
- 权限：`CGPreflightScreenCaptureAccess()` 快速探测 + `SCShareableContent.current` 真实探测（避免 preflight 缓存误判）。
- 捕获：按 `displayID` 匹配 `SCDisplay`，把屏幕内部坐标（左下原点）的选区矩形换算为**左上原点**的 `sourceRect`，`config.width/height = 逻辑尺寸 × backingScaleFactor`（Retina 物理像素），`SCScreenshotManager.captureImage` 出 `CGImage`。

### 2.5 选区遮罩（NSView 手绘）

`Views/CaptureOverlayView.swift`：
- `CaptureOverlayWindow`：`NSPanel` 风格的无边框透明窗，`windowLevel = .screenSaver`，`collectionBehavior` 覆盖所有 Space。
- `CaptureOverlayNSView`：`mouseDown/Dragged/Up` 记录选区，`draw()` 用 `CGContext` 绘制遮罩/选区/尺寸提示；`resetCursorRects` 提供十字光标；`keyDown` Esc 取消。
- 选区过小（<1px）视为取消。

### 2.6 编辑器（核心）

`Views/EditorView.swift` + `Models/EditDocument.swift`：

- **画布**：`Canvas` 绘制原始图（等比居中、只缩不放，保证像素清晰）+ 所有 `CanvasElement`；坐标在「图像坐标」与「视图坐标」间用 `scaleFactor`/`displayRect` 互转（`toImage/toView`）。
- **元素模型**（`CanvasElement` enum）：
  - `.stroke(StrokeElement)`：点集 + 颜色 + 线宽（画线 = 贝塞尔路径，实时渲染，松开即成一对象）。
  - `.bubble(BubbleElement)`：锚点 + 文本框 CGRect + 文本；白色底、黑字、带小尾巴引线、选中可移动/缩放/拖锚点、双击进入文字编辑。
- **工具**：`select / pencil / bubble` 三种状态，工具栏 6 按钮（撤回/重做/画线/标注/下载/复制）+ 画笔面板（系统调色板 + 粗细 2/5/9）。
- **撤销/重做**：`EditDocument` 双栈（undo/redo），上限 50 步，`snapshotBeforeChange()` 在每次修改前快照，新编辑清空 redo。
- **键盘快捷键**（local monitor，仅编辑页获得焦点时生效）：`⌘Z`/`⇧⌘Z`/`⌘S`/`⌘C`/`L`/`T`/`Del`/`Esc`。
- **导出**：`exportedImage()` 在原始截图上按顺序重绘所有元素合成 `NSImage` → `pngData`（NSBitmapImageRep）。
  - 下载：文件名 `zeroshot_yyyyMMdd_HHmmss.png`，按设置「每次询问」弹 `NSSavePanel` 或直接存默认目录（重名自动加 `-1`、`-2`…），保存后记录 `lastSavedPath` 并关窗。
  - 复制：`NSPasteboard` 写入 png 数据。

### 2.7 设置页

- `Views/SettingsView.swift`：`Form` 分组 —— 「快捷键与启动」（快捷键录制 + 开机自启）、「保存」（默认保存位置 + 询问开关）。修改即时生效、即时持久化，无保存按钮。
- `Views/ShortcutRecorderView.swift`：点录制框 → 本地 keyDown monitor 捕获；校验需至少一个修饰键；`Esc` 取消录制；「恢复默认」= `⌘⇧S`。
- `Views/SaveDirectoryRow.swift`：路径展示（可复制）+ 「选择…」`NSOpenPanel` + 「打开」Finder。
- `Models/SettingsStore.swift`：默认目录 `~/Downloads/zeroshot`（启动自动创建）；自启用 `SMAppService.mainApp.register/unregister`（注册失败回滚开关）。

**UserDefaults 键一览**

| Key | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `globalShortcut.keyCode` | Int | 1(s) | 快捷键键码 |
| `globalShortcut.modifiers` | Int | cmd+shift | 快捷键修饰键 |
| `launchAtLogin` | Bool | true | 开机自启 |
| `saveDirectory` | String | `~/Downloads/zeroshot` | 默认保存目录 |
| `askSaveLocation` | Bool | false | 保存时是否询问位置 |
| `lastSavedPath` | String | nil | 最近保存路径（菜单「打开最近截图」用） |

### 2.8 日志

`Services/ZSLog.swift`：`ZSLog(...)` 同时写 stderr 和 `/tmp/zeroshot.log`（追加）。排查问题先看该文件。

---

## 3. 关键实现细节（改代码前必读）

1. **坐标系统**：
   - NSScreen/SwiftUI 画布为**左下原点**；ScreenCaptureKit `sourceRect` 为**左上原点** → `ScreenCapture.topLeftRect` 负责换算。
   - Retina 用 `screen.backingScaleFactor`：`config.width/height` 乘缩放；`NSImage(size:)` 用逻辑尺寸保证 2× 物理像素。
2. **编辑器坐标**：所有元素存**图像坐标**，绘制/命中测试转视图坐标；`fitRect` 只缩小不放大。
3. **撤销粒度**：任何 `doc.elements` 的改动前先 `snapshotBeforeChange()`；`EditDocument.elements` 是 `@Published`，直接改下标数组即可驱动重绘。
4. **快捷键**：全局热键走 Carbon（设置里改 → 发通知 → 重注册）；编辑页内快捷键走 `NSEvent.addLocalMonitorForEvents`，返回 `nil` 表示吞掉事件。
5. **权限**：每编译一次系统会把 App 当作新程序，**需重新在「系统设置 → 隐私与安全性 → 屏幕录制」授权**（代码内已内置引导弹窗）。
6. **防重入**：`CaptureCoordinator.isActive` 保证同一时刻只有一个截屏会话；`isShowingAlert` 防止权限弹窗重复。
7. **构建配置**：`INFOPLIST_KEY_LSUIElement = YES`（无 Dock）、`MACOSX_DEPLOYMENT_TARGET = 14.0`、`PRODUCT_BUNDLE_IDENTIFIER = com.zeroshot.app`；工程用 `PBXFileSystemSynchronizedRootGroup`（新增源文件到 `Zeroshot/` 目录即自动纳入编译，无需改 pbxproj）。

**调试辅助环境变量**（进程环境，方便 CI/脚本）：

| 变量 | 作用 |
|---|---|
| `ZEROSHOT_AUTO_CAPTURE=1` | 启动 1.5s 后自动触发一次截图流程 |
| `ZEROSHOT_FAKE_PERMISSION=1` | 跳过真实权限检测（视为已授权），用于无授权环境的流程调试 |

---

## 4. 编译与执行

### 4.1 前置要求

- **macOS 14.0+**（运行/部署目标）
- **Xcode 16+**（工程 `objectVersion 77` + 文件系统同步组；Xcode 26 已验证）
- 命令行构建工具：Xcode 自带 `xcodebuild`。若 shell 找不到，使用完整路径：
  `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild`
- 无需第三方依赖、无需 CocoaPods/SPM。

### 4.2 命令行编译（推荐）

```bash
cd /Users/kin/Documents/10source/zeroshot

# Debug 构建（默认产物在 DerivedData）
/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild \
  -project Zeroshot.xcodeproj \
  -scheme Zeroshot \
  -configuration Debug \
  -derivedDataPath ./dist
  build

# Release 构建
/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild \
  -project Zeroshot.xcodeproj \
  -scheme Zeroshot \
  -configuration Release \
  -derivedDataPath ./dist
  build
```

> 工程未含 `.xcscheme` 文件，但单 target 项目 xcodebuild 会自动生成 scheme，`-scheme Zeroshot` 可直接使用；若个别环境报 scheme 找不到，可改用 `-target Zeroshot`（需同时加 `-configuration`）。

### 4.3 构建产物位置

- 默认（DerivedData）：`~/Library/Developer/Xcode/DerivedData/Zeroshot-*/Build/Products/Debug/Zeroshot.app`
- 也可用 `-derivedDataPath ./build` 显式指定输出目录，便于脚本取用：
  ```bash
  xcodebuild -project Zeroshot.xcodeproj -scheme Zeroshot -configuration Debug \
    -derivedDataPath build build
  # 产物：build/Build/Products/Debug/Zeroshot.app
  ```

### 4.4 运行

```bash
open path/to/Zeroshot.app
```

或 Xcode 图形化：打开 `Zeroshot.xcodeproj` → 选 target `Zeroshot` → ⌘R。

**首次使用**：
1. 启动后仅菜单栏出现图标（无 Dock 图标，属正常设计）。
2. 按 `⌘⇧S`（或点菜单「立即截图」）触发截图；未授权屏幕录制时按引导授权：
   「系统设置 → 隐私与安全性 → 屏幕录制」→ 打开 Zeroshot。
3. 授权后重新触发即可使用；**每次重新编译运行需重新授权一次**。

### 4.5 已构建副本

`dist/Zeroshot.app` 为一份可直接运行的已构建副本（arm64），配套 `dist/启动Zeroshot.command`（内容为 `open` 该 App，可右键打开运行一次）。

### 4.6 常见问题排查

| 现象 | 原因/处理 |
|---|---|
| 按快捷键无反应 | 检查屏幕录制权限；看 `/tmp/zeroshot.log` 是否有 `startCapture` 与 `permission check` 日志 |
| 截图全黑 | 未授权或授权后未重新编译（系统视为新程序），重新授权 |
| 快捷键被占用 | 设置页修改快捷键；全局热键注册失败不会崩溃 |
| 没有 Dock 图标 | 正常（`LSUIElement=YES`），仅菜单栏驻留 |
| 保存目录 | 默认 `~/Downloads/zeroshot`，可在设置中修改并自动创建 |

---

## 5. 里程碑对照（与需求文档）

- **M0（Demo）**：设置页 + 菜单栏入口 + 快捷键录制/持久化 + 自启 + 保存路径 —— **已实现**。
- **M1（MVP）**：⌘⇧S 选区、编辑页、6 按钮、撤销/画线/气泡、下载/复制、权限引导 —— **已实现**。
- **M2**：快捷键真正接入全局热键、设置项全链路生效 —— **已实现**（`GlobalHotkeyManager` + 通知联动）。
- **M3**：Toast、视觉规范、多屏深测、签名公证 —— 部分完成，待 Notarization。
- **M4（可选）**：长截图、贴图置顶、图形（箭头/矩形）等 —— 未开始。
